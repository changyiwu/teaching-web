# teaching-web（國中數學教學整合網）（專案藍圖）

> 本檔為跨 Agent 通用的專案藍圖（AGENTS.md 開放標準）。任何 Agent 的每個 session 都應先讀本檔＋`handoff.md`。

## 專案簡介

國中數學教學整合網：純靜態前端頁面（HTML5、CSS3、Vanilla JavaScript），以 HSL 色彩系統打造深色磨砂玻璃風格（Glassmorphic UI）。用於課堂智慧白板投影，涵蓋 6 冊 58 小節課程。外部資源為 Font Awesome 6 CDN 與 Google Fonts 的 `Outfit`、`Noto Sans TC`；隨堂備忘錄與各冊自訂快捷連結存於瀏覽器 `localStorage`。獨立的即時文字雲頁 `wordcloud.html` 例外使用 Firebase Firestore 同步課堂輸入。

線上網址：<https://changyiwu.github.io/teaching-web/>

## 關鍵時程

<!-- 目前無固定時程 -->

## 目標與路線圖

- [x] 階段一：首頁與 58 個教材頁骨架、GitHub Pages 自動部署
- [x] 階段二：規則統一為跨 Agent `agents.md`
- [x] 階段三：共用元件抽出——`curriculum.js`、`annotate.css/js`、`tools-sidebar.css/js`、`lesson-nav.css/js`、`materials/pending.css`
- [x] 階段四：小節完成狀態改為資料驅動；首頁新增跨冊搜尋與狀態篩選；修好「新增連結」自訂書籤
- [x] 階段五：教材圖片全面轉 WebP（11.4MB → 0.89MB）
- [x] 階段六：建置 `1-1-3`「整數的乘除與四則運算」（黏土定格動畫風，6 重點／6 互動／12 題）
- [ ] 階段七：在課堂智慧板實測課堂畫筆（捲動模式、觸控筆跡、4K 解析度下的落筆位置）
- [ ] 階段八：其餘佔位頁逐節建置（已完成 `1-1-4`「指數記法與科學記號」復古太空探險風、`1-2-1`「因數與倍數」台灣夜市水彩風、`1-2-2`「最大公因數與最小公倍數」8-bit 像素電玩風、`1-2-3`「分數的四則運算」手繪烘焙風、`1-2-4`「指數律」和紙摺紙工房風、`1-3-1`「代數式的化簡」手搖飲吧檯黑板菜單風、`1-3-2`「一元一次方程式」蒸汽龐克黃銅天平工房風、`1-3-3`「應用問題」復古偵探事務所風、`2-1-1`「二元一次方程式」復古車票印刷風）
- [x] 階段十：教材頁算式繪圖引擎抽成 `materials/math-canvas.js`；已完成的 9 個教材頁全部接上（`1-1-3`、`1-1-4`、`1-2-1`～`1-2-4`、`1-3-1`～`1-3-3`）。`1-1-1` 沒有用到任何共用工具，維持不載入；`1-1-2` 原本也不載，2026-09-04 重點 2 改成互動探索後用到 `wbrEq()`，已接上
- [x] 階段九：投影可讀性與窄螢幕版面修正——MathJax 上下標與數字字距（`1-1-4`）、CSS Grid／flex 溢出裁切（`1-1-1`～`1-1-4` 與首頁）；規則已收進〈開發約束〉9～11
- [x] 階段十一：`.value-display` 假捲軸修正——11 個已完成教材頁的互動卡數值列不再冒出多餘捲軸；規則已收進〈開發約束〉23
- [x] 階段十三：全站 logo 與 favicon 改用手寫 SVG——本站 `header-logo.svg`、`wordcloud.html` 內嵌 SVG，以及側邊欄三個外部專案（`exam-review`、`class-tools-2`、`class-score`）各自的標誌；規則已收進〈開發約束〉25
- [x] 階段十四：第一冊 11 節依〈開發約束〉26 回頭重評重點數（依據為課本 PDF 的主題／例題骨架＋章節架構圖）——7 節維持，4 節重拆：`1-2-2` 6→7（應用問題拆成 GCD 型／LCM 型）、`1-2-4` 6→7（指數律③與④分家）、`1-3-1` 6→7（四則運算與分數形式獨立）、`1-3-3` 7→8（速率與合理性分家）。順手把 `1-1-1` 的題數正規化為 10（刪掉重點 5 那題純機械的絕對值比大小），並清掉 `1-2-2`／`1-2-4` 共 14 條既有的算式溢出
- [x] 階段十二：`.value-display` 在 414px 下的 5 處 MathJax 算式真溢出全部消除（`1-1-2`、`1-1-3`、`1-1-4`、`1-2-3` 四頁的 `canvas.js`）；作法是把長算式拆成多段 `\( \)` 並以 `<wbr>` 接合，規則已收進〈開發約束〉24
- [x] 階段十五：第一冊全冊算式溢出歸零——補掃前四階段沒動過的 6 節，清掉 33 條（`1-1-3` 兩條在 1280px 也會壓到互動卡）；補齊 `1-1-1`／`1-1-2`／`1-1-3` 缺的 kerning（含 `^\circ` 的排除規則）；`.value-display` 的假捲軸修法推到 11 節全部。〈開發約束〉24 的斷段手法抽成 `math-canvas.js` 的 `wbrEq()`
- [x] 階段十六：第一冊全冊互動的「敘述與狀態相容性」掃描——依新增的〈開發約束〉27 掃 10 節 33 張互動卡 87 支滑桿，修掉 `1-2-3` 應用卡兩個情境印出的假等式（夾結果不夾輸入，26.8%／29.5% 的組合會中）、`1-2-1` 短除法在 n 為質數時落空的敘述、`1-3-1`／`1-1-2` 正數也被包括號的三處；`wbrEq()` 擴充成也在頂層加減號前斷開；`1-2-3` 通分插圖因「工具形狀切不出畫面上的線」重生（規則升級進〈開發約束〉28）
- [x] 階段十七：`1-1-2` 的兩張靜態卡改成真的互動探索（重點 2「湊整重組實驗台」、重點 5「絕對值防錯實驗台」），重點 2 原本的示範連 Q1 的答案都先算完送出去；規則升級進〈開發約束〉29。同輪修掉四處敘述（重點 1、重點 4、核心回顧兩條）、重點 4 因行內樣式蓋過 `.bracket-btn.active` 而「選取態其實沒變過」、殘留的字面 markdown 星號，以及三條被 `overflow:hidden` 真的裁掉的解析算式（`.explanation-box` 沒被〈開發約束〉14 的保險涵蓋）
- [x] 階段十八：第一冊全冊「評量解析框」的算式裁切歸零——依〈開發約束〉14 新補的那半條掃 11 節 140 個解析框，在 414px 抓到 21 條被 `.concept-section` 的 `overflow: hidden` 真的裁掉的算式（最嚴重 118.5px），**全部是行內 `\( \)`、顯示型一條都沒有**，改寫成顯示型 `aligned` 後歸零；同輪把 `.explanation-box` 的 overflow 保險補到 11 節全部（`1-1-1`／`1-2-2` 原本連 `.explanation-card` 的都沒有）。至此〈階段十五〉「第一冊全冊算式溢出歸零」的結論才真的涵蓋評量區塊

- [x] 階段十九：第一冊全冊「示範題與評量題撞題」清零（〈開發約束〉29 後半）——逐節對照 11 節 70 張互動卡與 140 題，修掉 3 處逐字相同（`1-3-2` 重點 6 的挑錯題就是 Q2、`1-3-3` 重點 7 的滑桿預設就是 Q1 的三個數字、`1-3-3` 重點 3 的「餅乾分裝」就是 Q1）、4 處可調出答案（`1-2-2` 重點 4／重點 7、`1-1-3` 重點 1／重點 2）、4 處重點整理逐字寫出正解（`1-1-4` 重點 2、`1-3-1` 重點 3／重點 5、`1-2-1` 重點 5）。同輪把互動回饋區（`.visual-feedback`，5 處）與核心回顧（`.summary-card`，2 處）的算式溢出也歸零——這兩個區塊過去從沒被掃過，規則收進〈開發約束〉31

- [x] 階段二十：依〈開發約束〉28 稽核第一冊全部 66 張插圖（不含 11 張漫畫），逐張對圖判讀。重生 9 張：工具形狀產生不了畫面上結果的 4 張（`1-3-2` 的天平兩盤皆空又被手按住、天平右盤空著卻顯示平衡、滾壓機「壓」出圓片；`1-2-2` 兩齒輪沒咬合且懸空無軸）、數量與比例對不上宣稱的 5 張（`1-2-3` 左塔實為 6 等分使 3/4＝6/8 不成立、`1-1-4` 指數塔畫成線性堆疊、`1-1-4` 十倍寬實測只有 1:1.9:2.5:4.7、`1-2-1` 三盤數量不等、`1-2-4` 外層 3×3 內層卻 4×4）；另修 17 處 alt 與畫面不符。同輪把 `1-1-1`、`1-1-2` 兩節 12 張舊世代插圖整批重畫——`1-1-2` 原本 6 張裡 5 張內容有誤（混入 ∫／Σ／π 與偽公式、畫的是別的重點、數線沒有 0 與負數），`1-1-1` 全節更是一張負數都沒出現過。新主軸：`1-1-1` 數線＋溫度計（水豚貼紙風）、`1-1-2` 溫度計（水彩日式動漫風，角色照該節漫畫寫死）。規則收進〈開發約束〉32、33

- [x] 階段二十一：`1-1-1`／`1-1-2` 插圖的數線與道具修正——數線箭頭規則釐清為「只在正向那端」（`capybara_opposite`／`capybara_summary` 兩端都有箭頭、`capybara_distance` 兩端都沒有，共重生 3 張；`capybara_numberline` 本來就對）。`1-1-2` 六張插圖原本全是「直立溫度計疊箭頭」且無刻度，`temp_laws` 更把「交換律結果相同」畫成兩支液面不一樣高；改為**依重點性質換道具**——位置類（重點 1、3、6）用水平溫度刻度條，符號類改用店裡真會發生的事（重點 2 兩支甜筒換順序疊球、重點 4 收據整組取消翻正負號、重點 5 絕對值機器：`-5` 投進去、`5` 吐出來，舉著減號牌的店員被圍欄擋在機器外，要等機器吐出正數才能動手）。規則收進〈開發約束〉34，並擴充 33、釐清 8

- [x] 階段二十二：插圖不再硬扛畫不出來的概念——`1-1-2` 重點 5 的插圖改成純情境圖 `shop_moment`（店員遞甜筒、灰貓在檯面上抬頭看），畫面不出現任何數字與符號，只保留場景與角色一致性。該重點教的是「絕對值內部先算完」與「\(-|5-8|\) 不能像 \(-(5-8)\) 那樣把負號帶進去」，兩者都沒有可畫的幾何，前兩代插圖（`temp_absolute` → `abs_machine`）因此都退化成在畫「絕對值的定義」——而那是 `1-1-1` 的內容。規則收進〈開發約束〉35

- [x] 階段二十三：第二冊開篇 `2-1-1`「二元一次方程式」建置完成（復古車票印刷風）。依〈開發約束 26〉從康軒 1 下備課 PDF 抽骨架（2 個主題、6 個名詞方塊、9 個例題、5 題自我檢核、4 大題自我評量）推導出 **8 個重點**——超出技能寫的 4～7 是因為主題 2 的三個解概念（判斷解／無限多組／限制條件）在課本各有獨立例題與獨立評量題，照〈開發約束 26〉不能併；反過來把課本 4 個化簡例題壓成 2 格，因為去括號、分配律、通分在 `1-3-1` 都教過。8 互動、16 題。驗收在 414px 與 1280px 兩檔、四類容器全部零溢出

- [x] 階段二十四：`2-1-2`「解二元一次聯立方程式」建置完成（沿用 `2-1-1` 的復古車票印刷風與同一組角色）。依〈開發約束 26〉從康軒 1 下 PDF 抽骨架（3 個主題、2 個名詞方塊、9 個例題、4 題自我檢核、1＋6 小題自我評量＋挑錯題）推導出 **8 個重點**——連兩節都是 8 是各節內容碰巧接近，不是沿用；本節的判斷分歧只在「消去前的整理」，移項化標準式（例8）與去分母（例9）動作不同、學生分別出錯，照〈開發約束 26〉字面拆成兩格。8 互動、16 題。驗收在 414px 與 1280px 兩檔、四類容器全部零溢出

- [x] 階段二十五：`2-1-3`「應用問題」建置完成（沿用車票印刷風），第二冊第 1 章三節到齊。依〈開發約束 26〉從課本抽骨架（1 個主題、4 個例題、5 題自我評量、2 題會考觀摩）推導出 **6 個重點**——比前兩節的 8 少，是因為本節只有 1 個主題、4 個例題；「題目問的不一定是未知數本身」在自我評量單獨佔一題，照〈開發約束 26〉獨立成格。6 互動、12 題，正解 A/B/C/D 各 3 題。驗收：414px 與 1280px 兩檔、四類容器零溢出；約 1000 個狀態掃邏輯、208 個極端狀態量版面。同輪抓到兩個真問題——Q7 兩個誘答宣稱的錯法實算出來對不上該選項（重算後改為 17 與 43）、回頭讀題機在 \(x<y\) 時印出「男生比女生多 \(-16\) 人」（違反〈開發約束 27〉，改成問句與算式一起翻向）

- [x] 階段二十六：`2-1-1`／`2-1-2`／`2-1-3` 三頁重複的 **10 個工具**抽進 `math-canvas.js`（淨減 228 行）——`drawTicket`、`drawBrace`、`drawStepRows`、`fitLines`、`inkItems`、`termItems`、`termTex`、`eqTex`、`sub`、`casesTex`。拆成兩個 commit：9 個逐像素相同的走純重構，`termItems` 因為 `2-1-1` 的首項負號被字距推成「- 5x」而另開一個修正。沒抽的有理由——主題配色是各節調色盤（且 `1-3-1`～`1-3-3` 也宣告了 `C_SKY`／`C_EMBER`）、`drawGate` 兩頁幾何刻意不同、`initQuizSystem` 的答案表本來就每頁不同。規則收進〈工作約定〉的抽取與驗收兩條

- [x] 階段二十七：`2-2-1`「直角坐標平面」建置完成（第二冊第 2 章開篇，畫風換成**復古航海製圖室**）。依〈開發約束 26〉從課本抽骨架（2 個主題、3 個 Key point 方塊、7 個例題、4 題自我檢核、4 大題自我評量＋挑錯題、2 題會考觀摩）推導出 **9 個重點**——比前三節的 8／8／6 都多，是因為本節課本佔 15 頁，且平移的正推／回推、四象限的符號規則／符號運算判象限，課本各給了獨立例題與獨立評量題，照〈開發約束 26〉不能併。9 互動、18 題，正解 A5 B4 C5 D4。驗收在 414px 與 1280px 兩檔、四類容器全部零溢出；4317 個狀態掃邏輯零異常；重點 5／6／7／9 的算式各用具體數字獨立反算過。同輪抓到四個真問題——`chart_distance` 兩張候選的鉛垂尺都穿過了 x 軸（量出來就不是「到 x 軸的距離」）、漫畫候選 2 的 x 軸左端也長了箭頭、`drawArrow` 的箭頭只有 5.4px 寬在投影下看不見、依〈開發約束 29〉稽核抓到 4 處互動可調出評量答案

- [x] 階段二十八：`2-2-2`「二元一次方程式的圖形」建置完成，**第二冊第 2 章完結**（沿用 `2-2-1` 的復古航海製圖室風與同一組角色）。依〈開發約束 26〉從康軒 1 下 PDF 抽骨架（2 個主題、4 個名詞方塊、**10 個例題**、5 題自我檢核、5 大題自我評量＋挑錯題、3 題會考觀摩）推導出 **10 個重點**——本節是全冊例題最多的一節，且章節架構圖列了 7＋1 條學習目標；把「與兩軸的交點／判象限」與「通過原點」拆開（解題動作一個看圖一個代零，自我評量各佔一題），把「圍成的面積」獨立成格（例 10 ＋自我評量 5⑵）。10 互動、20 題，正解 **A5 B5 C5 D5**。驗收在 414px 與 1280px 兩檔、四類容器全部零溢出；**7468 個狀態**掃邏輯零異常；截距／交點／面積／反求係數四組公式各用獨立算法反算全中（象限判定另以取樣法逐條比對 273 條直線）

- [x] 階段二十九：`2-2-1`／`2-2-2` 兩頁重複的**坐標平面 7 個工具**抽進 `math-canvas.js`（淨減 168 行）——`drawPlane`、`axisArrow`、`drawDot`、`drawFlag`、`dashLine`、`quadName`、`quadOf`。逐符號比對的結果：`axisArrow`／`dashLine`／`quadName`／`quadOf` 四個逐字相同直接搬；`drawDot` 只差在把 `r || 6` 提成區域變數，行為相同；`drawPlane` 的刻度字型參數化成 `cfg.tickFont`（預設 `f(600, 11.5)` 保住 `2-2-1`，`2-2-2` 傳 `f(700, 13.5)`），`cx` 可省略取畫布中線，其餘位置參數改成必填（共用檔不能依賴 `2-2-2` 的 `PL_*` 頁面常數，該頁改用一個 `PLANE` 設定物件）；`drawFlag` 兩頁尺寸不同（26/18/21/15 對 24/16/19.5/14）且不成比例，**統一回 `2-2-1` 的原版尺寸**。顏色一律由呼叫端傳入，預設取 `INK`／`MUTED`（`INK` 的 `#cbd5e1` 剛好等於兩節寫死的 `CH_SLATE`）。A/B 驗證：`2-2-1` 9 張畫布 × 40 個狀態**簽章全同**；`2-2-2` 10 張中 6 張全同，4 張有旗子的差異經逐像素比對確認只落在旗面上（`canvas-trace` 的 diff bbox 為 `None`、`canvas-sol` 為 19×12px）

- [x] 階段三十：`2-3-1`「比例式」建置完成（第二冊第 3 章開篇，畫風換成**微縮建築模型工坊**——航海製圖室已連用 `2-2-1`、`2-2-2` 兩節）。依〈開發約束 26〉從康軒 1 下 PDF 抽骨架（3 個主題、11 個例題、4 個 Key point、5 題自我檢核、5 大題自我評量＋挑錯題、2 題會考觀摩，章節架構圖把 3-1 拆成 5 個獨立方塊）推導出 **11 個重點**——比前幾節的 8／8／6／9／10 都多，是因為本節內文佔 13 頁、例題是全冊最多。11 互動、22 題，正解 **A5 B6 C5 D6**。畫風依〈開發約束 32〉數內文情境詞決定：長度類（公里 23、公分 18、公尺 10）加比例尺 6、平面圖 4 壓倒性，主軸取「量測與按比例縮放」。驗收：**5155 個狀態**掃邏輯零異常、**8287 次**數學獨立反算全中、414px 與 1280px 兩檔四類容器零溢出、**30786 個 `\frac`** 零未約分。同輪抓到四個真問題——`texFrac()` 不化簡使分數比印出 \(\frac{9}{3}\)、倒數那一步印成化簡後的 \(\frac{5}{4}\) 與下一步相乘的數字對不上、步驟編號隨模式跳號、依〈開發約束 29〉稽核抓到 Q8 兩個選項落在重點 4 互動的滑桿範圍內。生圖兩輪：第一輪 11 張裡 4 張沒過關（尺被當成其中一根木條、長出第二副護目鏡、兩排總長實測差 7.4%、兩根木條長度幾乎相同），改寫成可驗證的幾何後重生

- [x] 階段三十一：`2-3-2`「正比與反比」建置完成，**第二冊第 3 章完結**（沿用微縮建築模型工坊風與同一組角色）。依〈開發約束 26〉從康軒 1 下 PDF 抽骨架（2 個主題、8 個例題、2 個動動腦、P.118 課本概念、5 題自我檢核、5 大題自我評量＋挑錯題、1 題會考）推導出 **10 個重點**——正比、反比各四格（意義／判斷／求值／應用），加「趨勢不等於正反比」與「固定哪一個量決定正比或反比」兩格；表格判斷與情境判斷**併成一格**（解題動作都是檢查比值或乘積是否固定），兩個動動腦互為鏡像**併成一格**。⚠️ 康軒這一版的 3-2 **不教正比的圖形**（章節架構圖把它放到第四冊〈一次函數〉），互動改用表格與倍數呈現。正比與反比三組重點（判斷／求值／應用）共用同一套 canvas 引擎、以 `mode` 參數切換。10 互動、20 題，正解 **A5 B5 C5 D5**。驗收：**16365 個狀態**掃邏輯零異常（含獨立反算）、**45817 個 `\frac`** 零未約分、414px 與 1280px 兩檔各 320 個狀態四類容器零溢出。依〈開發約束 29〉稽核抓到 Q3、Q11 的選項數對落在求值台的滑桿範圍內，改題目數字。**生圖首次改走本機 ComfyUI（Z-Image Turbo）**——OpenAI API 額度用完；插圖 10 張、漫畫 1 張，漫畫因模型數不準 2×4 方陣，分鏡由「方磚改排」改成「小火車計時」

- [x] 階段三十二：`2-3-2` 的 10 張插圖與暖身漫畫改用 16GB 桌機的 **Z-Image Turbo 完整版（bf16）** 重生，畫面設計沿用上一輪（上一輪是 8GB 筆電的 int8 量化版）。3 輪共 76 張候選；卡最久的兩張是 `prop_pour`（前兩輪 8 張都把倒漆的人畫成少年，改寫成「畫面左半邊／右半邊」才對）與 `prop_loop`（改寫成「兩段直線接兩個半圓」才畫出完整橢圓軌道）。7 段 alt 依新畫面改寫並從瀏覽器讀回核對。提示詞與每輪 seed 收在 `comfyui/2-3-2/`（不進 repo）

- [x] 階段三十三：`2-4-1`「認識一元一次不等式」建置完成（第二冊第 4 章開篇，畫風換成**復古遊樂園海報**——模型工坊已連用兩節）。依〈開發約束 26〉從康軒 1 下 PDF 抽骨架（2 個主題、5 個例題、1 個 Key point、1 張習慣用語對照表、5 題自我檢核、5 大題自我評量＋挑錯題、2 題會考）推導出 **8 個重點**——「\(\ge\)、\(\le\) 的意義」與「一元一次不等式的定義」分兩格（一個判斷真假、一個分類，學生分別出錯）；「範圍內的整數解」課本沒有例題、只佔自我檢核一題，併進兩個不等號的圖示，**留給 4-2 獨立成格**。畫風依〈開發約束 32〉數情境詞：買 11、票 11（4-2 更是買 84、票 32），主軸「買票與預算」，**4-1、4-2 兩節共用**同一組角色（小晴、阿哲）。8 互動、16 題，正解 **A4 B4 C4 D4**。驗收：**20,699 項**邏輯檢查（每個狀態獨立重算）零異常；414px 與 1280px 兩檔靜態內容加 128 個極端狀態，四類容器零溢出；撞題掃描只抓到一處脈絡不相干的 125（改成 132）。生圖走本機 Z-Image Turbo，插圖 8 張、漫畫 1 張：「頭頂剛好碰到橫桿」兩輪才勉強過（仍隔約 7px，alt 不宣稱碰到），「頭頂夾在兩根橫桿之間」兩輪 10 張全失敗，依〈開發約束 35〉退回純情境圖。提示詞、seed、API 工作流程與送件腳本收在 `comfyui/2-4-1/`。**同日依使用者回饋修三處**：暖身漫畫第 4 格的小丑立牌少了橫桿（整張重生，四格立牌一致）、爆米花插圖的手捏硬幣不自然（改成打開零錢包）、套圈圈的圈不立體又被畫得比瓶子大（改成手掌大小的厚圈）

- [x] 階段三十四：`2-4-2`「解一元一次不等式」建置完成，**第二冊第 4 章完結**（沿用復古遊樂園海報風與小晴、阿哲）。依〈開發約束 26〉從康軒 1 下 PDF 抽骨架（2 個主題、9 個例題、3 個 Key point、5 題自我檢核、3 大題自我評量＋挑錯題、2 題會考；課本註明新課綱**不解連寫兩個不等號的不等式**）推導出 **11 個重點**——乘除正數、乘除負數分兩格（架構圖、Key point、自我檢核都分開列）；「\(x\) 只在一邊的移項」「兩邊都有 \(x\)」「去括號」「去分母並圖示」各佔一格（例 3～6 各一題、自我評量 1⑴～⑷ 各一小題）；應用題拆成「取整數」（4-1 留下的伏筆）、「價目表」、「折扣門檻」、「兩個條件夾出範圍」四格。11 互動、22 題，正解 **A6 B5 C6 D5**。驗收：2,750 個隨機狀態零例外、零未約分；**3,570 個狀態**逐一獨立反算解全中；414px 動態 390 個極端狀態與靜態兩檔，四類容器零溢出；撞題稽核修掉 5 處（例題解出 \(x \gt -4\) 等於 Q7 答案、「\(4 \gt x\) 就是 \(x \lt 4\)」與 Q10 同數字、例題裡的 \(120\)／\(500\) 與 Q15、Q17 相同、Q21 的 \(600\) 撞到漫畫的中間式）。同輪依〈開發約束 37〉把 `iqWbr` 與數線工具抽進 `math-canvas.js`（`wbrRel`、`numLine` 系列、`textCenter`／`textLeft`），`2-4-1` 八張畫布 × 40 個狀態簽章前後全同。生圖走本機 Z-Image Turbo（PC-YI-SL），插圖 11 張、漫畫 1 張**一輪全部選定**，提示詞、seed 與泡的座標收在 `comfyui/2-4-2/`

- [ ] 階段三十五：`2-5-1`「統計圖表與資料分析」**頁面**建置完成（第二冊第 5 章，本章只有這一節；畫風換成**昭和熱血運動漫畫網點風**），**插圖 12 張與暖身漫畫尚未生成**。依〈開發約束 26〉從康軒 1 下 PDF 抽骨架（3 個主題、9 個例題、2 個 Key point、4 題自我檢核、4 大題自我評量＋挑錯題、2 題會考、一題多解）推導出 **12 個重點**——全冊最多，內文 23 頁。計算機 M+ 在架構圖單列一格，但它是工具、課本裡唯一的用途是算「值 × 次數」的總和，併進次數表的平均；例 7（次數表的中位數）與例 8（分組中位數所在組別）解題動作都是累加定位，併成一格用模式切換。畫風依〈開發約束 32〉數情境詞：身高／體重／球／仰臥起坐／跑走合計約 113、分數 62、支付僅 10，主軸取班級體適能檢測與小考。12 互動、24 題，正解 **A6 B6 C6 D6**。驗收：4800 個隨機狀態零例外；2212 項獨立反算（百分率與圓心角、跨組加總、組中點、基準差距、登記錯誤、次數表平均、分組平均、中位數、累加定位、眾數、分組歸屬）全中；414px 與 1280px 兩檔靜態內容加各 127 個極端狀態，四類容器零溢出。撞題掃描抓到 Q7、Q22 與重點 6、11 互動卡用同一套分組（\(150\)～\(160\)……），連同 3 處不相干的數字一起換掉。生圖留待 PC-YI-SL：這台 NB-YI 只有 8GB 的 int8 版，提示詞、角色設定與漫畫對白先寫進 `comfyui/2-5-1/`，HTML 每個插圖位置留了註解「插圖（桌機生成後放入）」

- [ ] 階段三十六：`2-6-1`「垂直、線對稱與三視圖」**頁面**建置完成，**第二冊的頁面全部到齊**（第 6 章只有這一節；畫風換成**彩色卡紙拼貼風**，角色是小芸、阿翔），**插圖 13 張與暖身漫畫尚未生成**。依〈開發約束 26〉從康軒 1 下 PDF 抽骨架（4 個主題、6 個例題、1 個 Key point、2 個問題探索、2 個動動腦、自我檢核 7 塊＋4 題是非、自我評量 7 大題＋挑錯題、4 題會考、2 題素養、一題多解）推導出 **13 個重點**——全冊最多。「等腰三角形的對稱軸」併進例 1 的找對稱軸（解題動作都是找一條摺了會重疊的線）；例 2（直的對稱軸）與例 3（斜的對稱軸）併成一格用模式切換，自我評量 5 本來就同一題；「多邊形與正多邊形」與「標示與對角線」分兩格（學生分別錯在「邊等長就是正多邊形」與「跳著寫頂點」）。畫風依〈開發約束 32〉數情境詞：摺 33、紙 25、剪 24，其餘都不到 10；`1-2-4` 已用過和紙摺紙，改用卡紙拼貼。13 互動、26 題，正解 **A7 B7 C6 D6**。驗收：7,586 個隨機狀態零例外、數值列零 NaN／未配對定界符；26 題的答案表、選項、解析「答案為 X」三方一致；三視圖四題的視圖與「拿走哪一塊前視圖會變」由程式計算核對；414px 與 1280px 兩檔靜態內容加各約 300 個狀態，四類容器零溢出；全頁 704 個算式零 MathJax 錯誤。同輪抓到四個真問題——三個動作按鈕（重來、清除剪痕、換一張視圖）放在 `bindPickGroup` 綁定的按鈕群組裡，點下去被當成選模式（`換一張視圖` 直接丟例外）；重點 12、13 的預設積木有兩個和 Q25 的右、左視圖完全相同（〈開發約束 29〉），四個預設全部重設計；「只有邊等長」的八邊形出現 172° 的角，看起來少一個頂點；三張畫布的標籤互壓。評量題的 28 張 SVG 由 node 腳本產生後注入（HTML 以 `<!--SVG:key-->…<!--/SVG-->` 包住，改圖可重注入）。生圖留待 PC-YI-SL，提示詞與漫畫對白在 `comfyui/2-6-1/`，HTML 每個插圖位置留了註解「插圖（桌機生成後放入）」

> 目前課程進度：58 節中 `1-1-1`、`1-1-2`、`1-1-3`、`1-1-4`、`1-2-1`、`1-2-2`、`1-2-3`、`1-2-4`、`1-3-1`、`1-3-2`、`1-3-3`（第一冊全冊）、`2-1-1`、`2-1-2`、`2-1-3`（第二冊第 1 章全章）、`2-2-1`、`2-2-2`（第二冊第 2 章全章）、`2-3-1`、`2-3-2`（第二冊第 3 章全章）、`2-4-1`、`2-4-2`（第二冊第 4 章全章）、`2-5-1`（第二冊第 5 章全章，插圖與漫畫待生成）、`2-6-1`（第二冊第 6 章全章，插圖與漫畫待生成）完成，第二冊頁面全冊到齊，其餘 35 節為佔位頁。

## 資料夾結構

```
teaching-web/
├─ index.html              # 首頁（跨冊搜尋、狀態篩選、自訂連結）
├─ script.js               # 首頁邏輯（不放完成狀態判斷式）
├─ style.css
├─ curriculum.js           # 6 冊 58 小節課程資料庫；完成狀態只寫這裡
├─ tools-sidebar.css/js    # 課堂工具側邊欄（共用元件）
├─ annotate.css/js         # 課堂畫筆（整頁座標，跟隨捲動）
├─ lesson-nav.css/js       # 教材頁上一節／下一節導覽
├─ materials/              # 58 個教材頁
│  ├─ math-canvas.js       # 教材頁共用的 Canvas 算式繪圖引擎（新頁一律載入，不要再複製）
│  └─ pending.css          # 56 個「待施工」佔位頁共用樣式
├─ background.png
├─ header-logo.svg         # 站台標誌與分頁圖標（手寫向量；見〈開發約束〉25）
├─ tools/                  # 驗收用小工具（不是網站的一部分）
│  ├─ nostore.py           # 送 Cache-Control: no-store 的靜態伺服器（預設埠 8765）
│  └─ shots.py             # 收 canvas toDataURL 存成 PNG（預設埠 8766，輸出 tools/_shots/）
├─ comfyui/                # 本機 ComfyUI 生圖的提示詞、每輪 seed，依節分資料夾（gitignore，只走雲端硬碟）；2-4-1 起另附 API 工作流程 workflow_api.json 與送件腳本 gen.py
├─ wordcloud.html          # 即時協作文字雲（Firebase；全域技能模板產生）
├─ README.md
├─ agents.md               # 本檔：專案藍圖
├─ handoff.md              # 交接檔（每次收工必更新）
├─ .agents/skills/math-interactive-material/SKILL.md
└─ .gitignore
```

## 同步層級（本專案初始化至第 3 層級）

| 層級 | 平台 | 位置 | 讀取時機 |
|------|------|------|---------|
| L1 | 本地（GDrive） | `agents.md`＋`handoff.md` | 每個 session |
| L2 | GitHub | https://github.com/changyiwu/teaching-web （公開，push `main` 自動部署 Pages） | 指定時 |
| L3 | Obsidian | `teaching-web/專案工作流程.md` | 有需要時 |

## 三個檔案的職責（依「時效性」分家，不是依「詳細程度」）

| 檔案 | 時效 | 寫入方式 | 放什麼 |
|------|------|---------|--------|
| `handoff.md` | **只對下一個 session 有效**，過期即丟 | 每次收工整份重寫 | 做到哪、下一步、**這次**的暫時 workaround |
| `agents.md`（本檔） | **長期有效**，每個 session 都適用 | 只有規則本身變了才改 | 目標、路線圖、常設規則、結構 |
| Obsidian／`git log` | **歷史**：發生過什麼、為什麼 | 只增不刪 | 決策紀錄、踩坑完整版、逐次進度 |

驗收標準：**`handoff.md` 整份刪掉，不應損失任何長期資訊**——會的話代表該升級進本檔卻沒升級。

**本檔不要出現的東西**：❌ `## 最近進度`／逐次工作紀錄、❌ 決策理由與踩坑完整版。2026-08-03 移除了 `## 最近進度`，內容逐條比對後已在 L3 筆記的〈🗓️ 最近更動紀錄〉——**是主動移除，不是遺漏，不要補回來**。踩過的坑只把**結論**收斂成一條祈使句寫進〈工作約定〉，原因留 L3。

## 工作約定

- 任何 Agent、任何電腦：**開工先讀 `handoff.md`，收工必更新 `handoff.md`**
- 修改共用檔案前先讀最新內容，避免覆蓋其他 Agent 的變更
- **`git status` 冒出一大批「變動」時，先跑 `git diff HEAD` 再動作**：本 repo 放在 Google 雲端硬碟裡，雲端硬碟會把舊版的 `.git/index` 同步回來，於是 `git status` 列出幾十筆變動、逐檔比對 HEAD 卻是 byte-identical。用 `git reset` 重建索引就好（內容零損失），**不要 checkout，那才會真的丟掉工作**。同理，判斷版本一律以 `git diff HEAD`／`git log` 為準，不要靠讀檔或看時間戳——雲端硬碟可能餵出過期內容。若 `git status` 是 `MM` 但 `git diff HEAD` 空的，多半只是 LF/CRLF 差異，`git add --renormalize .` 消掉即可
- 所有回應與文件使用繁體中文；涉及檔案操作時回報完整產出位置
- Windows 指令優先使用 PowerShell
- 收工前檢查程式碼是否含 API key、網址 Token、學生姓名等敏感資料
- 只 stage 本次任務相關檔案，**不使用無差別的 `git add .`**；僅在使用者明確授權時 commit 與 push
- 本機預覽窗格（Browser pane）會供快取的舊版 CSS/JS，改完在那裡看不到效果是正常的；**`navigate` 帶 `force` 與加 query 參數都擋不住**，驗收一律用 **`python tools/nostore.py`**（會送 `Cache-Control: no-store`），不要用 `python -m http.server` 的預設行為
- **`tools/nostore.py` 的服務根目錄寫死成 teaching-web**（`ROOT` 由腳本位置回推），`cd` 到別的資料夾再啟動它是沒有用的——它會照樣服務本站，於是「檔案明明在、卻回 404」，而且本站剛好也有 `index.html`，看起來像伺服器正常、只有資產壞掉。要預覽別的專案一律用 `python -m http.server <port> --directory <path>`
- Browser pane 沒有顯示在畫面上時 `screenshot` 會逾時（頁面不合成畫格）；要看 canvas 就起 **`python tools/shots.py`**，用 `toDataURL()` 把畫面 POST 過去存成 PNG 再讀檔，不必等使用者打開窗格
- **收回來的 canvas PNG 是透明底，判讀外觀前一定要先用卡片底色 `alpha_composite` 疊過**；直接 `convert('RGB')` 會把半透明填色變成全彩（`rgba(52,211,153,0.18)` 變成亮綠），文字跟底色同色就整片消失，看起來像 bug 其實是截圖流程的錯
- 教材頁的本機驗收一定要走 HTTP（`python tools/nostore.py`）；直接開 `file://` 在預覽窗格只會拿到靜態快照，`canvas.js` 不會執行、互動全部驗不到
- **Browser pane 沒顯示時，`getBoundingClientRect()` 的 `height` 會是 0，用合成的 `MouseEvent` 點 canvas 一定落空**：`canvasPos()` 用 `clientY - rect.top` 換算，rect 高度 0 就算不出座標。要驗 canvas 的點擊互動，暫時覆寫該 canvas 的 `getBoundingClientRect` 回傳 `{left:0, top:0, width: cv.width, height: cv.height}`，再用 canvas 內部座標 dispatch click，驗完還原。注意這與「量版面前先確認 `innerWidth` 等於預期」是兩件事——`innerWidth` 正確時 `getBoundingClientRect()` 仍可能回 0
- **Browser pane 的捲動不聽使喚**：`window.scrollTo`、`document.body.scrollTop`、`documentElement.scrollTop`、`scrollIntoView` 四種寫法在 `1-2-3` 上全部無效（讀回一律 0），只有 `1-1-2`／`1-2-1` 偶爾成功，原因未查明。驗收一律靠「量數字」而不是靠截圖，不需要真的把元素捲到畫面上
- **判定「有沒有假捲軸」要看「真的捲得動嗎」，不能只看 `scrollWidth - clientWidth`**：MathJax 的 `mjx-c` 字符盒會有幾 px 的墨跡外溢被算進 `scrollWidth`，於是明明可見算式的最右緣還差 0.1px 才到邊，卻回報溢出 2～3px。作法是寫 `el.scrollLeft = 99999` 再讀回來，讀到 0 就是捲不動、不會出現捲軸（純量測雜訊），讀到 N 才是真的有 N px 可捲。這是既有那條「用 `scrollWidth - clientWidth` 而不是 `offsetWidth - clientWidth`」的下一層
- **量行內算式放不放得下，基準是 `<li>` 的實際可用寬，不是卡片內容寬**：`.explanation-card` 在 414px 下內容寬 267px，但項目符號的縮排讓 `<li>` 只剩 **241px**（巢狀 `<ul>` 更只有 225px）。用 267px 判斷會得出「這條只溢出 1.6px，加個 `<br>` 讓它換行就好」的錯誤結論——實際上它自己一行也放不下，非改顯示型不可。量的時候取該 `mjx-container` 最近的非 inline 祖先，扣掉它的左右 padding
- **`.value-display` 只掃初始狀態會漏掉一半的溢出**：數值列的內容隨滑桿與按鈕變動，最寬的字串往往出現在極端值（最負的數多一組括號、因數最多的那個 n、指數相減變負數）。掃描要把每支滑桿掃 min／中／max 的笛卡兒組合、每顆按鈕點一輪，逐狀態記錄最差值。第一冊實測這樣多抓到 9 條初始狀態看不到的溢出
- **預覽窗格沒顯示時 `setTimeout` 會被 Chrome 的 intensive throttling 壓到每分鐘一次**，掃描迴圈只跑十幾個狀態就逾時（分頁隱藏超過 5 分鐘後觸發，`tabs_select` 把分頁切到前景也救不了，因為窗格本身沒顯示）。改用 `MessageChannel` 讓步就不受節流：`new Promise(r => { const ch = new MessageChannel(); ch.port1.onmessage = r; ch.port2.postMessage(0); })`，包成忙等到 `Date.now()` 差達標為止。實測 300ms 的等待確實只花 300ms，而 MathJax 也照常重新排版。**但量「排版之後的尺寸」時不要用這個忙等去等 MathJax**：`2-4-1` 每切一個狀態忙等 420ms 再量，128 個狀態跑到一半整頁卡死，連 `javascript_tool` 讀一個數字都逾時，只能重新載入。改成每切一個狀態 `await MathJax.startup.promise`（MathJax 3 的 `typesetPromise` 會串在它後面，resolve 就代表排完），再用 MessageChannel 讓步一次就量——同樣 128 個狀態不到 1 秒跑完，並且一次 `javascript_tool` 只掃 2～3 個互動
- **量版面時「顯示型」與「行內」算式要分兩輪量**：顯示型量 `mjx-container[display="true"]` 的 `scrollWidth - clientWidth`，行內量 `mjx-container:not([display="true"])` 相對於 `.explanation-card` **內容區**（要扣掉 padding）的左右緣，並排除 `.table-scroll` 裡的。只量顯示型會漏掉開發約束 16 那一半——`1-3-1` 第一輪就這樣漏掉兩條溢出 139px／114px 的行內算式
- **`curl ... | python` 在 Windows 會用 cp950 讀 stdin**，中文一律比對失敗（報 substring not found）。要檢查檔案內容就用 `io.open(..., encoding='utf-8')` 直接讀檔，不要走管線
- **幾百個狀態的「邏輯」掃描要先 stub 掉 `MathJax.typesetPromise`**：互動每重繪一次就排一次 MathJax 排版，連續跑幾百輪會把排版佇列塞爆、分頁直接沒回應（實測 441 個狀態跑到第 137 個就斷線，`javascript_tool` 回 Internal error 或 Promise was collected）。驗**邏輯**時把它換成 `() => Promise.resolve()`，改讀 `innerHTML` 裡的原始 LaTeX（此時 `\ne` 還沒被排版成 `≠`），882 個狀態幾秒就跑完；驗**版面**時再還原回去，只挑極端值的少量狀態慢慢量。兩件事分開跑，不要混在同一個迴圈裡
- **不要用截圖判斷「文字雲上某個詞有沒有畫出來」**：12px 的詞混在 50px 的詞裡幾乎看不見，肉眼會判成沒畫。一律監聽 wordcloud2 的 `wordclouddrawn` 事件、逐詞讀 `detail.drawn`
- 瀏覽器分頁未在前景時渲染會被節流，大量 canvas 迴圈驗證會逾時；先把分頁切到前景，且單批控制在 100 組參數以內
- **用 `subprocess` 接外部指令（生圖腳本等）的輸出時不要直接 `print`**：Windows 主控台是 cp950，子行程輸出只要含一個對應不到的字元就丟 `UnicodeEncodeError`，**整批工作會停在第一個項目**。用 `errors="replace"` 收下來還不夠——那個 `\ufffd` 自己也印不出去；要 `msg.encode("ascii", "replace").decode("ascii")` 再印。這是既有 cp950 那條（讀 stdin）的另一半
- **`print` 出去的字串本身也不能含非 cp950 字元**：`✓`、`✗`、`→` 這類符號在 Windows 主控台直接丟 `UnicodeEncodeError`，而且**是整支腳本中止**——若那行 `print` 排在實際動作前面，會出現「訊息印到一半、檔案卻沒被改」的假象，容易誤判成程式沒生效而重跑一次。驗證訊息一律只用 ASCII。這是 cp950 那兩條（讀 stdin、接 subprocess 輸出）的第三半
- **量對話泡內緣的「白色」不可以三個通道共用一個門檻**：暖色調底圖的泡內部實測是 `(254, 247, 233)`，藍色通道只有 233，用 `>= 243` 會一個泡都找不到（看起來像量測程式壞掉）。分通道給門檻，實測 `r>=240, g>=234, b>=218` 可用（不同批候選的泡會差幾階，`2-2-1` 那批是 `(248, 237, 218)`，門檻要放寬到 `b>=208` 才抓得到）。**反過來，泡旁邊有淺色的雲時要收緊**：`2-4-1` 重生的漫畫第 3、4 格，泡的白色跟雲連成一塊，量出來是整格 539×674；卡到 `r>=250, g>=250, b>=246` 才分得開。**另外，`-fit letterbox` 補上的白邊也是亮色**，會被連通區域當成「最大的亮區」，量出來變成 90×675 的兩條長條；掃描前要先把左右各 90px 的補白排除掉
- **量到的泡外框要先剔除尾巴再用**：連通區域會把泡的尾巴、思考泡的小圓一起算進 bbox，高度多出三到五成，`bubbles.json` 照抄會讓文字重心被拉到泡的下緣。作法是只留「寬度達本體最寬 40% 以上、且與最寬那一列相連」的區段，再取外框
- **把教材頁改吃 `materials/math-canvas.js` 前，要逐符號比對「整個 canvas.js」，不能只比第 2 節**：`1-3-2` 另有一份 `drawEqPanel`／`eqLine` 定義在第 3 節前面，只掃第 2 節會漏掉，接上共用檔後就變成重複宣告。作法是把兩邊的頂層 `function`／`const` 各自抓出來做逐字比對，**逐個符號**分成「完全相同／有差異／本頁獨有」三類。相同的才刪；有差異的要先看清楚差在哪，只有在「共用版是嚴格超集或更保守」時才能換掉（實例：`roundRect` 的 `Math.abs(w)` 只在 w 為負時才有差、`gcd` 的 `a || 1` 只在 gcd(0,0) 才有差、`1-2-4` 的 `measure`／`drawIt` 少了本頁用不到的 `seq` 與斜體分支），並實測那個分歧路徑真的走不到（可以覆寫該函式記錄實際傳入的參數）。刪完記得順手清掉描述那些工具的孤兒註解。**反方向（把幾頁重複的工具抽進共用檔）走同一套逐符號比對**，但多兩件事要先做：一是**掃過整個 `materials/` 樹**確認沒有別頁宣告同名符號（共用檔是每頁都載的，撞名就整頁掛掉）；二是**各節的主題配色不要抽**——`C_INK` 這類是每一節自己的調色盤，而且 `1-3-1`～`1-3-3` 也各自宣告了 `C_SKY`／`C_EMBER`，搬進共用檔會變成重複宣告，未來想換色的節也綁死了。共用函式需要顏色一律由呼叫端傳入（`drawStepRows` 的 `opts.color` 即是），值剛好等於共用檔既有常數的才可以直接換（`C_SLATE` 與 `INK` 同為 `#cbd5e1`，換掉逐像素相同）
- **共用檔跟頁面撞名、但兩邊語意本來就不同時，改名頁面那一份，不要硬併**：`1-2-3` 的項是平面的 `{ frac }`／`{ txt }`，跟共用檔的遞迴元件（`T`／`VF`／`FR`）不相容，硬轉會動到每一條算式的外觀；把頁面的 `exprWidth`／`drawExpr` 改名成 `termsWidth`／`drawTerms` 就好。**改名前一定要全檔搜過新名字，含函式內部的巢狀宣告**——第一輪把 `drawExpr` 改成 `drawRow`，而 `initEquivCanvas` 裡本來就有一個巢狀的 `drawRow`，它呼叫全域版時變成呼叫自己，整頁 `Maximum call stack size exceeded`。頂層宣告掃描看不到巢狀的那一份
- **驗收重構要做「改動前後的 canvas 像素比對」，別只看有沒有 console 錯誤**：把 `git show HEAD:...` 的舊版放到 `materials/<節>-ref/`（深度相同，相對路徑才不會跑掉），兩邊各自 `await document.fonts.ready` 後跑同一串操作（每支滑桿掃 min／中／max、每顆按鈕點一輪），把每個狀態的 `toDataURL()` 湊成一支簽章比對。**參照副本一定要連該節的 `style.css` 一起複製**——教材頁的 `index.html` 載入的是同資料夾的 `style.css`，少了它字型會退回替代字型，每一張 canvas 都對不上，看起來像重構改壞了。驗完把 `-ref` 資料夾刪掉。**但改的若是 `math-canvas.js` 本身，`-ref` 這招用不了**：副本的 `index.html` 載的是 `../math-canvas.js`，也就是改過的那一份，於是副本自己那份複本會跟它重複宣告。此時改用「同一支簽章腳本在改動前後各跑一次」——先把改動前每個狀態的簽章存進 `localStorage`（同一個 origin，換頁與重新載入都留得住），改完再跑一次比對，差異會直接指出是哪一個狀態的哪一張 canvas
- **`math-canvas.js` 的 `wbrEq()` 吃的是裸 LaTeX，不要自己包 `\( \)`**：它自己會把每一段包成 `\( … \)`，先包好再傳進去會長出 `\( \(x \)<wbr>\( {}= 24\) \)` 這種巢狀定界符，MathJax 不會報錯、console 也乾淨，畫面上只是多出幾個怪括號，很容易一路帶到上線。**同一類的還有 `drawChip` 的第 7 個參數**：它是 `bg` 色字串（`'rgba(…)'`），不是 alpha 數字，傳 `0.22` 進去會被當成無效色而靜默吃掉。改用共用檔的函式前先看一眼它的簽章，不要照著別的函式的參數順序猜。**回傳值的形狀也要看**：`drawStepRows` 的 `rows[].items` 吃的是**陣列**，而 `inkItems()` 回傳的是單一元件（`SEQ` 物件），直接塞進去會在載入時就丟 `items.forEach is not a function`、整頁互動全滅；既有頁面都寫成 `items: [inkItems(...)]`，照抄即可
- **挑生圖候選時「比例」要量，不要用目視**：`2-1-3` 的 `ticket_reject` 要「右邊那張只有左邊兩張的一半寬」，我目視判定某張候選是 0.85 倍而差點淘汰它，**實測是 0.48**——正好就是要的一半。畫面的透視與傾斜會讓眼睛嚴重失準，尤其是斜放在檯面上的物件。作法是用亮度門檻掃出每一欄的前景像素數，取連續區段當物件邊界再算寬度比（紙張在深色木頭上實測 `r>190 and g>160 and b>90` 可用；**不要三通道共用一個門檻**，暖色紙的藍通道只有 120 左右）。這是〈開發約束 28〉「逐項比對外形」的量化版
- 驗收 Canvas 不要用 JS 硬撐 `canvas` 的 CSS 寬度來放大（會撐破版面、截圖全黑）；改為把視窗縮到 992px 以下讓版面轉單欄，或直接讀 canvas 像素判斷內容有無溢出邊界
- 本機沒有安裝 `PyMuPDF`／`Pillow`；PDF 與圖片處理一律走 `file-toolkit` 技能建好的共用環境：`C:\Users\chang\AppData\Local\file-toolkit\.venv\Scripts\python.exe`
- **Bash 工具的 heredoc 會把反斜線減半**——不只正規表示式，**LaTeX（`\frac`、`\left`）、CSS 註解、Windows 路徑一律中招**：`\frac` 會變成換頁字元＋`rac` 寫進檔案裡，而且 Python 只會發 SyntaxWarning 不會擋下來。任何含反斜線的內容都不要用 `cat <<'EOF'` 寫檔，也不要塞進 `python -c '...'` 的單行指令（**同樣會減半**），一律改用 Write 工具落檔再執行
- **量版面溢出前先確認 `innerWidth` 不是 0**：預覽窗格尚未配置版面時所有 `getBoundingClientRect()` 都會回 0，掃描程式會回報幾千個假溢出。先 `resize_window` 指定寬度、確認 `innerWidth` **等於預期的那個數字**再量——不是「非 0 就好」：`resize_window` 之後若沒重新載入或沒等版面穩定，量到的可能還是上一檔寬度，2026-09-04 就有一輪 111 個狀態是在錯的寬度下掃的（`.value-display` 量到 215px，實際只有 166px），整輪等於白量。掃描迴圈開頭直接 `if (innerWidth !== 預期) throw`，讓它壞在明處
- **掃描時要略過 `display: none` 的元素，並記得先把解析框展開**：未作答的 `.explanation-box` 是 `display: none`，`getBoundingClientRect()` 全回 0，會被誤判成「向左溢出一個卡片寬」——`1-3-2` 第一輪就這樣收到 90 筆一模一樣的假溢出。掃描前先 `style.display = 'block'` 展開全部解析框（裡面的算式本來就要量），並在迴圈裡跳過 `r.width === 0 || r.height === 0`
- **`loading="lazy"` 的圖片在預覽窗格不會載入**：分頁不在前景時 Chrome 不觸發 lazy 載入，`scrollIntoView` 也沒用，掃描會回報整批圖片壞掉。驗收時暫時 `img.loading = 'eager'; img.src = img.src;` 重新觸發，確認 `naturalWidth` 與顯示寬度即可，不要因此把教材頁的 `loading="lazy"` 拿掉
- **掃算式溢出時不要對整份文件重跑 `MathJax.typesetPromise()`**：那會把已經排版好的內容裡、`mjx-assistive-mml` 中的 `<math>` 再排一次，長出巢狀的 `mjx-container`。那層盒子被 CSS `clip` 成 1px、畫面上根本看不見，掃描卻會拿它當可用寬去比，回報一整批「溢出 20～150px」的假警報——`2-1-1` 第一輪 35 筆裡有 29 筆是這樣來的。只對真正改過內容的節點 typeset，並在掃描迴圈裡跳過 `m.closest('mjx-assistive-mml')`
- **「幾 px 算可接受」不要憑感覺，先去量一個既有的完成節**：`2-1-1` 一度想放行「只溢出 1px」的分數，實測 `1-2-3` 在 414px 下顯示型算式是 **0 條**、容器可用寬 241px（在 `<li>` 裡）／273px（直接在解析卡裡），基準因此是零。量到不合格就改寫算式（縮短項數、拆 `aligned`），不要調 CSS 或縮小分數
- **多行推導塞進 `.value-display` 時，`wbrEq()` 要逐行套用**：整段字串丟進去只會在最外層斷開，中間那幾行的分數照樣是不折行的 `mjx-container`。作法是每一行各自 `wbrEq()` 之後再包成 `<span style="display:block">`
- **量 MathJax 的幾何尺寸前一定要 `await document.fonts.ready`**：MathJax 的網頁字型是非同步載入的，`MathJax.startup.promise` resolve 時字型往往還沒到，此時量到的是替代字型的尺寸——分數的框會比實際高出 5 成，看起來像括號框不住分數。2026-08-21 就因為漏了這一步，連續兩輪誤判 `1-2-3` 絕對值括號的成因。量完最好再 `setTimeout` 一小段等重排完成。另注意 `1-2-3` 的 `mjx-stretchy-v.mjx-c7C` 高度是**量出來的固定值**，前提是絕對值裡放單層分數；該頁若改放帶分數或多層分數，那兩個數字要照這條重量
- **判斷「會不會出現捲軸」一律量 `scrollWidth - clientWidth`／`scrollHeight - clientHeight`，不要用 `offsetHeight - clientHeight`**：後者的差值也包含 border，帶 `border-top: 1px` 的元素會回報 1px 的假捲軸——`1-1-2` 就有 3 個 `.value-display` 這樣被誤判。同理，捲軸寬度的假陽性也會混進 `offsetWidth - clientWidth`
- **做 MathJax 的前後 A/B 時，「等 2.5 秒」還不夠，要量到數字**不再變動**才算數**：`await document.fonts.ready` 之後量到的寬度仍可能是替代字型的，實測同一個狀態先後量到 316.59 與 333（差 16.4px），足以讓人以為改壞了字距。作法是連續量到相鄰兩次完全相同才收下（每次間隔 400ms、最多重試 6 次）。另外**「改動前」的字串一定要跟當下狀態逐字相符**——`1-1-2` 的 `bStr` 只有在 \(b<0\) 時才加括號，手寫 before 字串時多打一組括號就會憑空多出 14px，看起來像字距變了。比對前先用 `textContent` 確認兩版文字完全一致。**靜態版面掃描也一樣**：`2-4-2` 在 414px 第一次掃，表格只多出 5px／8px；字型穩定後重掃變成 14px／21px，還多抓到另外兩處。靜態掃描一律間隔 3 秒連掃兩次，兩次結果一致才收下
- **改完版面 CSS 要做同頁 A/B 才能斷定溢出是誰造成的**：用 JS 把這次改動的屬性暫時設回舊值量一次、還原再量一次，就分得清「原本就溢出」與「這次改壞」。`1-1-2` 實測 112px→104px（改後反而少 8px）、`1-2-3` 42px→42px，據此確認 5 處手機寬度溢出全是既有問題。沒做這一步會把既有毛病誤記成自己的迴歸，然後花時間去改沒壞的東西
- **判定版面修好了要跨「真實寬度」量，不要只掃 `zoom`**：`document.documentElement.style.zoom` 改的是有效 CSS 寬度，`zoom: 3` 在 414px 視窗等於 138px——比任何真實裝置都窄，量出來一片紅但沒有意義。實際要看的是 414px（手機）與 1280px（課堂投影）兩檔，用 `resize_window` 真的改視窗
- **課本備課 PDF 用「版面語法」抽骨架，不要人工翻頁抄**：康軒備課 PDF 的字級與字體是穩定的——`18.5pt` 深色（`2301728`）＝**主題標題**、`13.2pt` 深色且字體為 `DFHeiStd-W7` ＝**例題標題**、`14.5pt` 白色＝**名詞方塊**、每節首頁的 `❶❷❸` 那一段＝小節大綱。用 PyMuPDF 的 `page.get_text("dict")` 逐 span 讀 `size`／`font`／`color` 篩這三類，就得到「主題 → 例題」的完整骨架，那正是〈開發約束〉26 要的概念清單依據。**1 下的備課 PDF 沿用同一套字級**，另外多兩類白色 `Arial-BoldMT` 編號可以直接數：**20.2pt ＝例題編號**（`2-1-1` 數出 9 個）、**19.2pt ＝主題編號**（數出 2 個），比人工翻頁點例題可靠得多。**只靠 `size` 會誤收方塊內文**（同為 13.x 深色），一定要一起比對 `font`。另有各章的〈章節架構圖〉PDF（`01`／`06`／`11` 開頭那三份），逐條列出學習目標，是最權威的一份，動筆前先讀它。⚠️ **不要用 `grep` 找 `❶` 這類多位元字元**——這個環境的 shell 會讓字元類別失效，整份檔案全部命中，看起來像抽取程式壞掉
- **拆重點時把既有 canvas 引擎參數化，不要複製一份**：一個重點拆成兩個時，互動往往只是「同一份繪圖程式碼、開放不同的模式子集」。把 `initXxxCanvas()` 改成吃 `opts`（`canvasId`、元素 id 前綴、預設模式），呼叫兩次即可。**按鈕的 `querySelectorAll` 一定要收斂到自己那一組**（加 `data-*-group="gcd"` 之類的屬性再選），否則兩張畫布會搶同一批控制項，點 A 組會連 B 組一起改。**沿用「別處既有的」class 當樣式時，對方那支舊的 class 選擇器也要一起收斂**——只把自己這組寫成 `[data-abs-mode]` 是不夠的：`1-1-2` 重點 4 的 `initBracketCanvas` 原本寫 `querySelectorAll('.bracket-btn')`，新卡片沿用 `.bracket-btn` 只為了共用樣式，結果點重點 2 會清掉重點 4 的選取、**並把重點 4 的畫面重置回第一種情形**（新按鈕沒有 `id`，傳進去是空字串、落到 default 分支）。加新的一組之前先 `grep` 那個 class，把既有的選擇器一併改成 `data-*`。另外，若某個模式用不到某條滑桿（例如只剩乘／除模式時的「外層指數」），**要把那條滑桿從該張卡片的 HTML 拿掉**並在 JS 加 `if (nS)` 防護，不要留一條永遠停用的死 UI。實測 `1-2-2`、`1-2-4`、`1-3-1` 三頁都適用
- **`math-canvas.js` 的 `texFrac()` 不化簡分數，canvas 上的分數元件卻化簡**：`texFrac(6, 2)` 印出來是 \(\frac{6}{2}\)，而同一個值走 `reduce()` 的畫布元件印的是 \(3\)——同一個數在數值列與畫面上長相不同，而且畫面上還會出現「\(\frac{6}{2}\)」這種沒約分的分數。頁面自己包一層 `fTex(n, d) { const r = reduce(n, d); return texFrac(r[0], r[1]); }`，所有 LaTeX 分數一律走它。**驗收要掃過每一個狀態的 `innerHTML`**，用正規式抓出每一個 `\frac{a}{b}` 檢查 `gcd(a,b) === 1` 且 `b !== 1`——`2-2-2` 靠這一輪掃到 700 多個狀態才確認歸零。**同一條的另外兩半**：一是**輸入端的分數本身也要先化簡再顯示**（`2-3-1` 的分數比模式寫死分母 4 與 3，滑桿選到 9 就印出 \(\frac{9}{3}\)，252 個狀態中招）；二是**「倒數」那一步反而不可以化簡**——\(\frac{8}{10}\) 的倒數要印 \(\frac{10}{8}\)，印成 \(\frac{5}{4}\) 會同時跟說明文字「分子分母對調」和下一步實際相乘的 \(\frac{60}{80}\) 對不上。另外自己寫的分數元件要處理**分母為負**的情形（`FR(3, B)` 在 \(B<0\) 時會畫出分母帶負號的分數），一律先 `reduce()` 把負號提到分子
- **步驟列的編號不要寫死在 `name` 裡**：列數會隨模式改變（`2-3-1` 的比值計算台整數模式 3 列、小數模式 4 列），寫死就會出現 ①③④ 這種跳號。作法是名稱只寫文字、畫之前跑一次 `numberRows(rows)` 重編。同理，**列距也要跟著列數調**——3 列用 50 的列距會在下半部留一大片空白，4 列排滿分數又會互相疊到；真的塞不下就把畫布加高（該頁的 `canvas-val` 由 470 加到 540），不要縮字級
- **`javascript_tool` 回報 45 秒逾時，不代表腳本沒跑完**：它只是「回傳」超時，頁面裡的迴圈通常已經跑到底。把計數與結果掛在 `window` 上（`window.__S.n`、`window.__S.hits`），逾時之後下一個呼叫再讀一次就拿得到——`2-3-1` 三次逾時全是這樣，128／87／106 個狀態其實都掃完了。反過來說，**掃描迴圈一定要把狀態存在 `window` 上而不是只放在區域變數裡**，否則逾時等於整批重跑
- **驗收腳本從 `.value-display` 讀回 LaTeX 時，要先把 `wbrEq` 插的 `{}` 與 `\text{…}` 的右大括號一起清掉**：`wbrEq` 會在每個斷點後面補一個空的 `{}`，而 `\text{ 的比值}` 的右括號會卡在 `比值` 和 `=` 中間，於是 `/x\s*=/`、`/比值\s*=/` 這種 pattern 全部落空，**幾百上千筆一起「失敗」，看起來像頁面壞掉**。`2-3-1` 為此誤報過 972 筆與 1501 筆。清法是 `innerHTML.replace(/<wbr>/g,'').replace(/\\\(|\\\)/g,' ').replace(/\{\}/g,' ')`，並讓數字的正規式同時吃 `\left(…\right)` 包起來的分數。**判斷是不是自己的解析壞掉，看失敗數是不是剛好等於掃描狀態數**——是的話一定是 parser，不是頁面
- **`math-canvas.js` 的 `sub()` 只替負數加括號，用在「並置相乘」時會讀成另一個數**：它是給「\(= 3\)」這種位置用的，正數不加括號；寫成 `2${sub(p)}` 時 \(p=3\) 會印出 `23`，被讀成二十三。代入相乘一律自己包一個 `mul(v) { return '(' + v + ')'; }`，正負數都加括號。這個錯誤在 console 與邏輯掃描裡完全看不出來（字串合法、數值也對），**只有把 canvas 收回來用眼睛看才抓得到**
- **抽共用工具時，「有差異」的符號分兩種收法：能參數化的參數化，不能參數化的挑一個版本統一並說明**：`drawPlane` 兩頁只差刻度字型，抽成 `cfg.tickFont` 並讓預設值等於舊版，兩頁都逐像素相同；`drawFlag` 兩頁的四個尺寸（26/18/21/15 對 24/16/19.5/14）**不成比例**，硬要參數化就得傳四個數字、共用檔反而更難讀，於是統一回較早那一版，讓已定稿的那節保持不變、較新的那節接受這個外觀變更。**判準是「哪一節已經上過課／已經定稿」**，不是「哪一版比較新」。統一之後要用 diff bbox 確認差異真的只落在該物件上（`2-2-2` 實測 `canvas-sol` 的 bbox 是 19×12px，正好一面旗子）
- **`-ref` 副本那招對「改共用檔」無效，但簽章可以存進 `localStorage` 跨頁比對**：兩節的教材頁同源，在 A 頁存 `sig_2-2-1`、B 頁存 `sig_2-2-2`，改完共用檔重新載入再比一次即可。掃描狀態要用**固定種子的亂數**（LCG）而不是笛卡兒全掃：四支滑桿的卡片全掃是 6561 個狀態，固定種子的 40 個隨機狀態已經足以抓出繪圖原語的任何改變，而且改動前後掃到的狀態序列完全一樣
- **新節要自訂全域符號（色票、工具函式）前，先確認不會撞到 `math-canvas.js`**：共用檔是每個教材頁都載的，撞名就整頁掛掉。動筆前跑一次 `grep -nE "^(function|const|let) +<名字>" materials/math-canvas.js`；`C_SKY`／`C_EMBER` 這兩個名字另外還被 `1-3-1`～`1-3-3` 各自宣告過。保險的作法是給本節的色票一個專屬前綴（`2-2-1` 用 `CH_`），**共用函式需要顏色一律由呼叫端傳入**，不要在共用檔裡放主題色

- **用行號對檔案動刀前，先自己讀檔算行號，不要照抄 `sed` 輸出的視覺位置**：`sed -n '600,604p'` 印出來的第一行若只有空白字元，看起來像不存在，會讓後面每一行的行號都少算一。而且**同一個 repo 裡的檔案行尾不一定一致**（`1-1-1/index.html` 是 LF、`1-2-2/index.html` 是 CRLF），`io.open(..., encoding=...)` 的通用換行轉換會把 `\r\n` 讀成 `\n`，於是「寫回去要用哪種行尾」變得不確定。作法是 `io.open(p, 'rb').read().decode('utf-8')` 自己 `split`，寫回時 `newline=''` 原樣送出，並在動刀前用 `assert` 檢查要刪的頭尾兩行內容真的符合預期
- **用 Python 腳本改中文內容時直接寫中文字面，不要手打 Unicode 逃逸序列**：為了閃 cp950 而把整句 alt 寫成 `\uXXXX`，等於在沒有校對能力的狀態下逐字打碼——2026-09-05 就把「金鈴鐺」（`\u9394`）打成「金鈴鈔」（`\u9214`），寫進 HTML 之後才從瀏覽器讀回來抓到。腳本檔本身是 UTF-8、Python 3 也預設以 UTF-8 讀原始碼，直接寫中文毫無問題；真正怕 cp950 的只有`print` 到主控台那一步（既有那三條），做法是**不要印中文**，而不是把要寫進檔案的內容也編碼掉。**改完中文內容一定要從瀏覽器讀回 `alt`／`textContent` 對一次**，不要只信「寫入成功」
- **動作按鈕（重來、清除、換一題）不要放進 `bindPickGroup` 綁定的 `.btn-group`**：它會把群組裡每一顆 `.pick-btn` 都當成選項，點動作按鈕時讀到的 `data-*` 是 `null`，模式變成 `null`／`NaN`——`2-6-1` 的「換一張視圖」因此直接丟例外，另外兩顆則是默默把狀態弄壞、畫面看起來只是怪。動作按鈕另開一個 `btn-group-row`，裡面的 `.btn-group` 不給 id。**隨機狀態掃描要把按鈕也點進去**，只調滑桿抓不到這種錯
- **用本機 ComfyUI 生圖一律照 `agent-comfyui` 技能**（原始檔 `C:\Users\chang\我的雲端硬碟\agents\agents-lazy-guide\skills\07-comfyui\SKILL.md`，完整教學在同 repo 的 `guides/07-連接-ComfyUI.md`）：不開 Desktop 視窗的 API 模式啟動、從生過的 PNG 取出工作流程、Z-Image Turbo 的提示詞脾氣（中文提示、負面提示無效、數不準方陣、服裝互換、一次跑 4 個 seed）都寫在那裡。技能清單裡沒有它時，直接讀原始檔
- **用 ComfyUI 生圖時，提示詞與每輪的 seed 一律存進 `comfyui/<節>/`**（已在 `.gitignore`，靠雲端硬碟跨機）：2026-09-11 那輪的插圖提示詞只放在 session 的暫存資料夾，候選 PNG 的 metadata 又只留在那台電腦的 ComfyUI output，換到另一台重生時兩者都拿不到，只能從 alt 反推重寫。漫畫救得回來，是因為 `_raw.png` 放在雲端硬碟的 `teaching-comic/output/`——PNG 的 `prompt` 區塊就是整份工作流程。每輪記下 seed、改了什麼、為什麼沒過，下一輪才不會重踩
- **撞題掃描（〈開發約束 29〉第五種形式）要比對原始 HTML，不要比對排版後的 `textContent`**：MathJax 排版完之後，`\(x \ge 3\)` 在 DOM 裡已經變成一堆字符盒，拿 LaTeX 片段去搜 `textContent` 永遠搜不到，會回報一片乾淨。作法是 `fetch(location.href, { cache: 'no-store' })` 抓回原始 HTML、`DOMParser` 解析，再拿每題題幹與正解選項裡的 `\( … \)` 片段，去搜 `.explanation-card`／`.interactive-card`／`.summary-card` 的原文（只比對含 3 個以上英數字的片段，否則單一數字會滿地命中）。**比對前先把 `input` 元素移除，並要求片段前後不是數字**：滑桿的 `min="120"`、`max="240"` 屬性會被當成內容命中，`\(x \lt 45\)` 也會被片段 `x \lt 4` 當成前綴命中——`2-4-2` 第一輪 14 筆裡有 4 筆是這樣來的
- **ComfyUI 候選圖的奶油色邊框偶爾會長出淡色的浮水印狀字跡**（`2-4-1` 的 `ineq_popcorn` s101 右下角）：縮到 220px 看不見，但選定前要把四個角落放大看過。要用邊框底色蓋掉時，先把覆蓋範圍裁切放大確認——第一次蓋的矩形切進角色褲管約 5px，得拆成「褲管下方一整條＋褲管右側一小塊」兩塊蓋
- **Z-Image 提示裡，同一件道具只能寫一次**：角色設定寫「斜背紅色圓形零錢包」、場景又寫「雙手捧著零錢包」，`2-4-1` 6 張候選全部畫出兩個包（一個捧在手上、一個還掛在背帶上）。場景要動到某件隨身道具時，那張改用拿掉該道具的角色設定（`comfyui/2-4-1/illustrations.json` 的 `characters_no_purse`），並在場景寫明「身上只有一個」。**尺寸不要用比喻，用身體部位定**：「像小游泳圈一樣有厚度」讓套圈圈的圈比瓶子還大，改成「直徑約一個手掌寬、比瓶身窄、只套得進瓶頸」才縮回夜市的尺寸。**要角色「剛好碰到」某個高度時，把道具高度綁在角色身上**：只寫「頭頂碰到橫桿」時柱子被畫得很高、間隙 70～130px，補上「柱子不高，橫桿剛好和少女頭頂一樣高」後縮到約 40px，但仍碰不到——這種「剛好」的幾何別期待一次到位，旁白與 alt 要照實寫

## 開發約束

1. 網頁用於課堂智慧白板投影，需維持高對比、易讀、美觀及清楚的 hover 微動畫
2. 首頁與教材頁維持無伺服器架構，使用者自訂資料保存在客戶端 `localStorage`；獨立即時互動頁可依全域技能規範使用 Firebase Firestore
3. 除非使用者明確要求，不引入 Tailwind CSS 或其他 CSS 框架，維持 Vanilla CSS
4. 不在程式碼中硬編碼帳密或 Token，也不把本機儲存資料上傳至分析平台
5. 外部連結必須使用 `target="_blank" rel="noopener noreferrer"`，避免課堂使用時覆蓋目前分頁
6. 小節完成狀態**一律只寫在 `curriculum.js`**（`"status": "completed"`），不要在 `script.js` 寫判斷式
7. **教材頁是 `<body>` 自己捲動**，不是文件捲動；跟捲動位置有關的功能要同時支援兩種來源（`annotate.js` 已處理）
8. 教材圖片一律使用 WebP（漫畫寬 1080、插圖寬 640），並加 `loading="lazy"`。品質**依畫風的紋理密度分兩級**：
   - **平面風格**（黏土定格、日式動漫、水豚等大色塊）用 **q85**；漫畫品質原為 88，2026-08-07 實測降到 85 可少 13～16% 體積且投影下看不出差異
   - **高頻紋理風格**（復古印刷網點、水彩紙紋、黑板粉筆紋等）用 **q75**；2026-08-20 實測 q75 比 q85 少 32% 體積，平均色差僅約 1%，且差異全落在不帶資訊的紙紋／網點雜訊上。再往下降（q70、q65）體積幾乎不再變小，畫質卻繼續掉，所以 **q75 是轉折點，不要再往下調**
   - 只剩 WebP、找不到原始 PNG 時可以直接重壓：實測 q85 WebP 再壓 q75 的二次失真（平均差 3.86/255）比從 PNG 單次壓 q75（4.79/255）還小
   - **8-bit 像素風算高頻紋理**（dithering 的棋盤點跟印刷網點同一類），插圖用 **q75**；2026-08-20 實測省 31%、2 倍放大下與 q85 看不出差別
   - **畫風同時符合兩級時以「有沒有紙紋」為準，走 q75**：`1-1-2` 的「水彩日式動漫」既像平面大色塊、又帶水彩紙紋，2026-09-04 那輪判成平面用了 q85。2026-09-05 實測同樣 6 張改 q75 省 **39%**（569KB → 347KB）、平均色差僅 3.6～3.9/255，**連刻度條上的 `-8`／`-6` 數字在 3 倍放大下都看不出差別**。水彩、粉彩、色鉛筆這類「看得到紙的纖維」的畫風一律歸高頻紋理
   - **但漫畫一律維持 q85**：漫畫上有後製排入的中文對白，字的邊緣禁不起再壓；插圖沒有文字才可以降
9. **CSS Grid 一律不要留 min-content 底線**：`1fr` 的最小值是 min-content，卡片裡只要有一段不能斷行的內容（MathJax 算式、長數字按鈕）整欄就會被撐破外框，而 `.concept-section`／`body` 是 `overflow: hidden`，超出的部分直接被裁掉。欄寬固定寫 `minmax(0, 1fr)`，有最小寬度需求就寫 `minmax(min(300px, 100%), 1fr)`
10. **flex 容器不要用 `align-items: center` 裝比它寬的內容**：溢出會左右各分一半，左邊那一半連橫向捲動都捲不到，等於內容消失。改用 `align-items: stretch`，需要置中的元素自己加 `margin-inline: auto`
11. **MathJax 預設字距在投影下不夠用，每個教材頁都要補這三條 kerning**：上下標 `mjx-msup > mjx-script { margin-left: 0.18em }`（實測預設 gap = 0，`0ⁿ` 的天然墨跡間距只有 0.03em）、數字內部 `mjx-mn > mjx-c + mjx-c { margin-left: 0.09em }`（`10000`、`0.001` 會黏成一團）、canvas 上自己畫的指數 `POW_KERN = 0.17`。**但頁面若用 `^\circ` 寫溫度，要加一條排除規則**：`mjx-msup > mjx-script:has(mjx-c.mjx-c2218) { margin-left: 0 }`。度數是數字的單位、本來就該貼著數字，套上 0.18em 會被推開成「\(-25\) °C」（實測 gap 由 0 變成 3.41px）。`\circ` 是 U+2218 RING OPERATOR，MathJax 給它的 class 固定是 `mjx-c2218`。這條排除規則**依賴 CSS `:has()`**，課堂用的 Chrome／Edge 支援；舊瀏覽器會忽略整條規則、度數多被推開 3.41px，不會壞版
12. **短除法一律「除到商是質數為止」**：課本的短除法停在商已經是質數的那一步（180 停在 `3)15`、梯形下面留 `5`），不要一路除到商為 1；`2-2` 最大公因數／最小公倍數也沿用同一個呈現方式
14. **顯示型算式 `\[ ... \]` 太長會直接壓到右欄的互動卡上**：兩欄之間只有 2rem（32px）的 gap，`1-2-3` 實測有一條算式溢出 95px，整段蓋在互動卡上。長算式要拆成 `\begin{aligned}` 多行（在 `&=` 前換行），並補一條 `mjx-container[display="true"] { overflow-x: auto }` 當保險。**這條保險要同時掛 `.explanation-card` 與 `.explanation-box`**——後者是評量的解析框，預設 `display: none`、掃描時容易整批漏掉，`1-1-2` 因此留了三條被 `.concept-section` 的 `overflow: hidden` 真的裁掉的行內算式（`concept6` 那條 `\overline{CD}` 切掉 77px）。2026-09-04 已補到 11 節全部。**但要知道這條保險其實修不到解析框的主要毛病**：實測 11 節 140 個解析框在 414px 下被裁掉的 21 條算式**全部是行內 `\( \)`，顯示型一條都沒有**——保險只管顯示型，真正的修法是照開發約束 16 把行內改寫成顯示型 `aligned`，改完才吃得到保險。所以掃解析框一定要連行內一起量（量的基準是 `<p class="explanation-desc">` 的內容區，414px 下只有 228.6px），只量顯示型會回報一片乾淨。**也別因為某節上一輪剛掃過就跳過它**——`1-1-2` 上一輪才整理完、交接檔記載「414px 被裁切 0」，這一輪仍抓到 2 條（溢出 31.8px 與 6.1px）；`overflow-y` 會連帶變成 hidden，記得加 `padding-block` 以免切掉分數的上下緣。**`aligned` 的第一行也要以 `&` 開頭**——`aligned` 的總寬是「左半欄最寬 ＋ 右半欄最寬」，第一行若放完整算式（沒有 `&`）就等於把它整條塞進左半欄，即使後面每行都拆過，總寬仍舊爆掉；`1-3-1` 實測 414px 下 15 條顯示型算式全部需要捲動，改成第一行 `& 原式`、其餘 `&= …` 之後歸零
13. **教材頁的 canvas 在投影下只有約 0.7 倍**：容器上限 1200px，兩欄版面下 540px 的 canvas 實際只顯示約 380px。內容密集的 canvas（百數表這類格子陣列）字級要比其他 canvas 大一級（實測 17px → 20px），否則投影在智慧板上讀不到
15. **`.sign-table` 在窄螢幕會被 `overflow: hidden` 裁掉**：`.concept-section` 是 `overflow: hidden`，414px 下表格實測溢出卡片 34px、整欄直接消失且捲不到。表格一律包一層 `<div class="table-scroll">`（`max-width: 100%; overflow-x: auto`）。**不可以直接對 `<table>` 下 `overflow-x`**——那需要 `display: block`，一旦不是 table 就失去欄寬同步，`thead` 與 `tbody` 的欄位會對不齊
16. **行內算式 `\( ... \)` 太長同樣會被裁掉**（開發約束 14 的另一半）：`mjx-container` 是 inline-block、內部不折行，接在中文引言後面時起點被推到句中，更容易超出卡片。窄螢幕下判準是「這條式子單獨一行放不下嗎」，會的就改成顯示型 `\[ ... \]`（吃得到 14 的 `overflow-x` 保險），必要時再拆 `aligned`。不要靠對行內 `mjx-container` 加 `overflow-x` 解決——那會讓 inline-block 的基線改成下緣，行內數學與文字的對齊全部跑掉
17. **教材圖表的長度／面積必須與數值成正比**：比大小、成長衰減這類主題**不可以用對數尺度**——等距的階梯很好看，但 \(a^2\) 看起來只比 \(a^1\) 長一點，實際上是它的 \(a\) 倍，等於把要教的東西抹平。值小到快看不見時就讓它看不見（保留 1～2px 並標數值），那正是要傳達的訊息
18. **幾何圖形的頂點字母與邊長標示一律畫在圖形「外側」**：為了避開碰撞而把標示塞進圖形內部，字會浮在填色的三角形上，被讀成「那一塊的標記」而不是邊長；同一張圖有些標示在外、有些在內更糟。頂邊要同時放頂點字母與邊長時疊成兩排（近邊那排放頂點、外面那排放邊長），並把整個圖形往下移，讓外排不會貼到 canvas 標題——`1-3-3` 實測邊長排放在 `oy-30`、圖形上緣要拉到 `oy=86` 才不會讓 \(x\) 看起來像標題的副標
19. **canvas 上的分數要用算式引擎（`VF`／`FR`）畫成真的分數**，不可以退化成 `24 ÷ 7`、`x ÷ 10` 這種除法寫法：同一頁的 MathJax 已經是分數，canvas 寫成除法等於同一個東西給學生兩種長相
20. **顯示型算式太長時，先想能不能把算式本身寫短，再考慮拆 `aligned`**：\((x+7) \times 2\) 改寫成 \(2(x+7)\) 就省下三個字元、單行放得回去。拆行會把一條完整的等式切成兩段，讀起來比原本差，是最後手段不是第一手段
21. **重點整理不要把特例講成通則**：「訣竅是設較小的那個當 \(x\)」「相等關係幾乎都是總金額」這類句子會讓學生以為別的做法是錯的。某個做法只是比較好寫（不是唯一正確）時要明說**兩種都對**，並把另一種也實際算一次；表格若寫「常見的設法」，必須同欄寫出「題目給了什麼條件」才站得住。**「只會」「一定」「都是」這類字眼要當成紅旗回頭檢查**——`1-3-3` 的「相等關係只會從兩個地方來：時間相同或距離相同」上線後才被抓出來，改成「常見情況」才對
22. **互動探索要能單獨讀懂**：問題型的互動必須在畫面上寫明**求什麼**，用到未知數 \(x\) 之前一定要有「設⋯⋯為 \(x\)」那一步。不可以因為左欄的重點整理已經講過就在 canvas 上省略——課堂投影時學生看的是右邊那張圖。**同一條也管「算式本身」**：不可以在 canvas 上寫「算式見下方數值列」而把式子丟給 `.value-display`，那等於整張圖只剩標籤。`2-1-1` 第一版的化簡工作台、驗票機、驗票閘門、解的清單機四張都犯了這個毛病，逐步推導、正在檢查的式子、正在檢驗的方程式全都要**畫在畫布上**（`math-canvas.js` 的 `drawExpr` 已經會自動縮字級塞進畫布），數值列只是同一件事的可複製文字版。逐行推導要留「左欄放步驟名與這一步在做什麼、右側放算式」的固定分欄，算式再寬也壓不到說明字。**反過來，左欄的中文說明也要量寬折行**——`2-1-2` 的「未知數搬到左邊，移項要變號」在 13px 下實測 161px，而左欄只有 150px，直接頂到算式的起點。作法是逐字量 `measureText` 折成最多兩行（步驟名放上面、說明放下面兩行），不要靠縮字級硬塞：那條說明在投影下只剩 0.7 倍，字級再降就讀不到了。步驟列數不固定時（8 或 9 列）列距也要由畫布高度自動配，寫死列距會讓最後一列掉出畫面
23. **`.value-display` 這類數值列不要只寫 `overflow-x: auto` 就收工**：依 CSS 規範 `overflow-x` 一旦不是 `visible`，`overflow-y` 會自動變成 `auto`，MathJax 的 inline-block 只要比容器高 1px 就冒出一根直向捲軸；它吃掉的 8px 又把同列內容擠到放不下，再引出一條橫向捲軸。而 `::-webkit-scrollbar` 若只設 `width` 沒設 `height`，橫向捲軸會用系統預設約 15px，在互動卡上看起來像一條紫色長條——比它要捲的內容還顯眼。四件事要一起做：`::-webkit-scrollbar` 補 `height: 8px`、容器加 `flex-wrap: wrap` 讓它先換行、明確 `overflow-y: hidden`、補 `padding-block: 2px` 讓那 1px 有地方放。**不可以改用 `scrollbar-width: none` 把捲軸藏掉了事**——手機寬度下有幾處是真的溢出，藏掉捲軸等於把「這裡還有東西沒顯示」的線索一起藏掉，是遮不是修。**橫向也要補 `padding-inline: 2px`**：`mjx-c` 的字符盒有幾 px 的墨跡外溢（實測 `offsetWidth` 9 對 `scrollWidth` 12），被算進 `scrollWidth` 就冒出一條捲不出東西的捲軸；補一點左右 padding 讓它有地方放，真的放不下的算式則照常換行。另外 MathJax 的無障礙 MathML（`mjx-assistive-mml`）是 `position: absolute` 且被 `clip` 成 1px、畫面上看不見，但它的 `width: auto` 同樣會撐大捲動範圍，要用 `.value-display mjx-assistive-mml { width: 1px !important }` 夾住。⚠️ `padding-block` 與 `padding-inline` 各 2px **讓數值列的可用寬少了 4px**，新頁面估算行內算式放不放得下時要先扣掉
24. **互動卡數值列（`.value-display`）的長算式要拆成多段 `\( \)`，用 `<wbr>` 接合**：`mjx-container` 是不折行的 inline-block，一整條 `A = B = C` 包成一段時，窄螢幕下整塊都放不下（實測 414px 溢出 42～104px）。作法是**在每個關係／運算子前斷開**，後段以 `{}` 開頭維持該運算子的字距——`\(A\)<wbr>\({}= B\)<wbr>\({}= C\)`；連乘則是 `\(-3\)<wbr>\({}\times (-5)\)…`。接合一定要用零寬的 `<wbr>` 而不是空白：空白會多塞一個字距，`<wbr>` 則讓寬螢幕的排版與原本**逐像素相同**（`1-1-2`／`1-1-3`／`1-1-4`／`1-2-3` 四頁 A/B 實測差 0.00px），只有真的放不下時才斷行。這是開發約束 16 在互動卡裡的具體作法：那裡的欄位比解析卡窄得多（1280px 下只有 356px、414px 下只有 215px），改成顯示型 `\[ \]` 並不適用。共用實作是 `math-canvas.js` 的 `wbrEq()`，斷點是深度 0 的 `=`／`+`／`-`（深度同時算 `{}`、`()`、`[]`，且只斷二元運算子），所以沒有等號的算式也斷得開。**但它不認得絕對值的 `|`**——`|` 不在深度計算裡，`|a + b| = c` 會被斷在絕對值正中間，變成兩段各帶一根孤立的直線；含 `|…|` 的算式不要用 `wbrEq()`，自己在等號處寫 `\(左式\)<wbr>\({}= 右式\)` 即可（`1-1-2` 重點 5 的絕對值實驗台即是）。**驗收要把每顆模式按鈕都點過、逐行推導的每一步都走過**，不能只量預設狀態——`1-3-1` 的 `11x-2[3x-(5x-4)]` 只在切到那個模式時才出現，前四輪掃描因此都沒抓到（與開發約束 27 的驗收要求是同一件事）
25. **Logo 與 favicon 一律手寫 SVG，不要用生圖技能產生**：logo 要在 16px（分頁圖標）到 64px（頁首）之間都清楚，還要精確吃住本站的 HSL 色票。生成式模型給的是點陣圖：縮到 16px 糊成一團、色彩只能「接近」、透明底要另外去背，改一個色或挪 2px 只能整張重生（見〈跨專案依賴〉那條「不要用遮罩改圖」），而且每次 `_raw` 都計費。手寫 SVG 是向量、色票逐字精確、可吃 CSS 變數跟著深色主題走、`git diff` 讀得懂，體積通常只有點陣圖的 5%～25%（實測 11.7 KB → 5.0 KB、96.2 KB → 3.6 KB）。**發光效果用 `radialGradient` 疊在底下做，不要靠 `feGaussianBlur` 濾鏡**——favicon 的渲染路徑對濾鏡支援不一，而徑向漸層到哪都畫得出來。重畫前先把原圖疊卡片底色看過，改完要在 16/32/64/180px 四檔做 A/B，因為 16px 下好不好讀跟 180px 完全是兩回事。**單檔部署的互動頁**（`wordcloud.html`）把 SVG 直接內嵌，favicon 用 `data:image/svg+xml,` 百分比編碼（比 base64 PNG 小很多），並把 SVG 內部的 `id` 加頁面前綴（例：`wc-`），否則會跟頁面既有的 `id` 相撞。生圖技能留給教材插圖、四格漫畫、簡報底圖這類大尺寸、有機紋理、不要求精確幾何的用途。早期 Safari 對 SVG favicon 支援不完整，課堂用 Chrome／Edge 不受影響；真要保險再補 `rel="mask-icon"`
26. **新節的重點數量要先列「概念清單」推導，不可以沿用上一節的數字**：技能 `SKILL.md` 第 3 節寫的是「由 AI 依教材內容自主拆解」，從來沒有訂死 6 個；但第一冊 `1-1-2`～`1-3-2` 連續九節都是 6，已經漂成預設值（`1-1-1` 其實是 5、`1-3-3` 是 7）。危險在於**三個數字是綁死的**——6 重點 → 6 互動 → 12 題，重點數一沿用題數也跟著沿用，等於教材的資訊密度由版面格式決定，而不是由課本內容決定。作法是動筆前先列出本節的概念清單、每條寫明依據（課本小標／例題群／課後自我評量的題型），**列給使用者過目後才決定拆幾個重點**。判準是**彼此獨立的新觀念有幾個**：解題動作不同、學生會分別出錯的各佔一格；只是同一件事換個寫法就併成一格；在課本只佔幾行但在自我評量單獨佔一題的算獨立觀念（`1-3-3` 的「解與情境的合理性」即由此從 6 變 7）。互動與題數跟著重點數走（每重點 1 互動、2 題）。第二冊起各節份量差異會比第一冊大，照 6 套會讓短的節灌水湊格、長的節被壓縮
27. **滑桿的範圍要跟畫面上寫死的敘述相容，掃過每一組組合都要成立**：canvas 上的說明文字（「上山比較慢，花的時間長」）與解析卡的結語（「不能直接把兩個速率平均」）是寫死的，但滑桿彼此獨立，使用者調得出讓那些句子變成假的組合——`1-3-3` 的上山時速可到 6、下山可低到 2，一調就出現「上山比下山快」，畫面與滑桿互相矛盾。**情境在物理上不成立的組合要擋下來**：畫一句紅字說明為什麼不成立、一句說怎麼調回去，`tex` 回 `\text{（…情境不成立）}`，解析卡寫出當下的兩個數值（`1-3-3` 的 `meet` 模式與 `round` 模式都是這個寫法，照抄即可）。**驗收要把每支滑桿的全部組合掃過**，不是只看預設值——`1-3-3` 的 round 模式是 5×7×6＝315 組
28. **教材插圖裡的工具，形狀必須能產生畫面上的結果**：`1-2-3` 的通分插圖原本畫「平行刀片的滾輪切刀」配「放射狀的楔形切線」——滾輪只切得出平行線，工具與結果是兩回事，投影在課堂上一眼看得出不對。動筆前先問「這個工具真的會做出這個結果嗎」。**而且不能只查「這類工具存不存在」就收工**：第一輪查到 pie divider 確實有賣就過關了，卻沒把產品照片跟生出來的圖對著看——真品是「與派同直徑的外環＋中央輪轂＋末端在環前收住的扁刀＋環上握把」，生出來的是沒有環、沒有握把、兩手捏中央小軸的風車骨架，連施力位置都不對。要逐項比對外形（有沒有外框、刀刃形狀、握持位置、相對尺寸）再寫進生圖提示。**器材的中文名要查實際行內名稱、不要自己造詞**——「放射狀分派器」是造的，台灣叫**蛋糕分片器**，可分 6 塊／8 塊是現成規格。要數的物件同樣受〈跨專案依賴〉那條「不超過 9」限制（**插圖與漫畫一體適用**），課本數字太大就改用同構的小數字：本例把 3、4 通分成 12 改成 2、3 通分成 6，並用**餡料顏色分組**表示原本切幾塊（左派 3 紅＋3 奶油＝兩半、右派 2＋2＋2＝三份），旁邊表格的原始例子不動。**還要看「人在畫面裡安不安全」**：分片器那一版形狀對了，但女生的手落在刀具下方，看起來會被切到。**能用更單純的工具就用**——最後改成普通披薩滾刀（單一刀盤＋木柄），一刀通過圓心滾過去、三刀切出六等分，既天然只產生放射線，也讓「刀片數要對上切線數」這個問題整個消失；手的位置要在提示裡寫死（持刀的手握木柄高處、在刀盤後上方，其餘的手平放檯面，沒有任何一隻手在刀具下方或前方）。**人和器材的關係也要逐項對過**：`2-4-2` 上線後使用者抓到四張——纜車畫成開放式座椅、人露在車廂外；雲霄飛車倒掛時兩人整個掉出車廂；蹺蹺板的把手沒接在木板上、少年也沒坐上去；彈珠水杯大到跟上半身一樣高。選圖時逐張問五件事：人在不在設施裡面、有沒有安全裝置、手握的東西是不是接在器材上、身體是不是真的坐在（踩在）器材上、日常物品和手或頭比起來是不是正常大小。提示要寫成看得出來的狀態：「完全密閉、有屋頂四面牆和門的車廂，人隔著玻璃窗」「U 形安全護肩壓住上半身」「把手用螺絲鎖在木板上」「屁股坐在木板上」「一整條連續、沒有斷開的鋼軌，四個輪子壓在鋼軌上，車底和鋼軌之間沒有空隙」——最後這句是第二輪才補的：換上的雲霄飛車雖然扣好了護肩，使用者又指出車底那段軌道沒接上迴環、車也不像在軌道上，**修掉一個問題的新候選要把五件事重新問一遍**。第三輪使用者又指出兩個人的護肩長得不一樣、高度也不同：**同一種裝置在畫面裡出現兩份時，提示要寫死「兩副完全一模一樣：形狀、顏色、大小、高度相同，並排對齊」**，選圖時逐張比對兩份的外形細節（扣環、按鈕）與頂端高度。**尺寸要綁在比物件還小的身體部位**：寫「只有一個手掌高、比頭小」，杯子仍約和頭一樣高；改成「高度大約一個拳頭、不到臉的一半」才縮成一般水杯的大小。另外，雲霄飛車加了護肩之後，6 張全都畫成正要衝進迴環、沒有倒掛——「安全」和「上下顛倒」模型只顧得了一個，教學上不需要倒掛時就接受，alt 照畫面寫
29. **互動探索卡必須真的可互動，而且示範用的例子不可以跟同節評量的題目相同**：技能 `SKILL.md` 訂的是「每個重點配一個視覺互動探索」，但寫著寫著容易退化成一張排版漂亮的靜態文字卡——`1-1-2` 六個重點裡有兩個是這樣（重點 2「運算規律精妙應用示範」、重點 5「絕對值防錯避坑指南」），整張卡沒有 canvas、沒有滑桿、沒有按鈕，內容從頭到尾不會變。**判準很簡單：這張卡有沒有任何一個東西是學生能動的**；沒有就不算互動探索，要嘛補上可調的參數，要嘛老實把它併進左欄的重點整理，不要擺在互動卡的位置上冒充。**更嚴重的是示範題與評量題撞題**：重點 2 那張卡把 \((-34) + 2508 + (-66)\) 從交換律、結合律一路算到答案 \(2408\)，而正下方的 Q1 問的就是同一條式子——學生看完示範直接抄，那題完全失去形成性評量的作用。**新頁完稿前把每個重點的「示範例子」與「該重點的兩題」對照一次**，數字相同就換掉示範的那個；補互動時也要順手確認，原本被示範送出去的答案是不是回來了。**「示範例子」不只是預設值，還包括滑桿調得到的範圍、寫死的情境敘述，以及元件的個數**：`1-3-3` 重點 7 的滑桿預設 60／80／100 正是 Q1 的三個數字，畫面直接印出方程式與答案；`1-1-3` 重點 2 的符號判斷器不只個數與 Q2 一樣是 5 個，連 `values` 都是 `3,5,7,9,11`，全部點成負號就等於把那題答完；`1-1-3` 重點 1 的互動卡與 Q2 是同一個水庫情境，而 \((-6)\times(-5)\) 剛好落在滑桿範圍內。**擋法優先序是：改互動卡的示範數字 → 收滑桿範圍 → 最後才改題目**，題目數字若非改不可，優先挑「答案不變、只把數量級推出範圍」的改法（`1-2-2` Q2 的磁磚 8×6 改成木板 80×60，答案仍是 12 塊）。**驗收時把每顆模式按鈕都點過**——`1-2-3` 重點 6 那兩條溢出只在切到「果汁」「圖書館」模式時才出現。**撞題還有第三種形式：原式明明不同，整理後的中間結果卻逐字相同**——`2-1-2` 重點 7 的互動第三題是 \(6x = 4x + y + 5\)、該重點 Q2 的第一式是 \(4x = 2x + y + 5\)，兩者整理出來的③都是 \(2x - y = 5\)，而 Q2 問的正是整理之後的解。逐步型的互動要比對的不只是原式，還有**它一路印出來的每一個中間式**。**另外，解析裡只要宣稱「這樣錯會算出 X」，就要真的把那條錯路算一次**：`2-1-2` Q6 原本寫「漏掉括號會算出 \(x=-1\)」，實際重算 \(2x+3\times4-3x=5\) 得到的是 \(x=7\)，連帶那個誘答選項的數字也是錯的。同輪還有兩處「某某選項是因為某某錯法」的敘述算出來對不上該選項的數字。誘答不是隨手編的陪襯，它要能被學生的錯誤真的產生出來，否則解析等於在教錯的東西。**第四種形式：互動如果提供一份「候選清單」（可選的式子、可選的情境、可選的地標），那整份清單都是示範例子**——`2-2-1` 重點 9 的符號推算盤有 6 個候選式，其中 \(E(s^2,\ st)\) 正是 Q18 要學生判的那個點，把象限按到「第二」就直接印出「第四象限」。**稽核時要三樣一起比對：互動的預設值、滑桿調得到的整個範圍、以及候選清單裡的每一項**。`2-2-1` 這一輪照這個作法掃出 4 處——除了上面那個，還有重點 1 調到 \(m=n\) 時回饋直接印出 Q2 的答案、Q4 的 \((5,-1)\) 與 Q7 的 \(P(-6,4)\) 都落在對應互動的滑桿範圍內。四處都照優先序處理：互動的邊界情形（\(m=n\)）是探索的重點不能拿掉，所以改題目；另外三處把題目數字推出滑桿範圍即可（\(7\)、\(-8\) 都超過 \(\pm 6\) 的上限），答案字母全部維持不變。

   **第五種形式：左欄的「重點整理」與末尾的「核心回顧」也是示範例子**——`2-2-2` 的重點 8 為了說明特例，寫了「兩點的 \(x\) 坐標相同（如 \((6,0)\) 與 \((6,3)\)）\(\Rightarrow\) 是鉛垂線 \(x = 6\)」，而 Q16 問的正是「通過 \((6,0)\) 與 \((6,3)\) 兩點的直線方程式」——題目與答案<strong>逐字</strong>都寫在同一頁的左欄。稽核不能只比對互動卡，要**把 `.explanation-card` 與 `.summary-card` 的全文抓下來，逐題拿正解選項的文字與題幹裡的每一個數對去搜**；`2-2-2` 這樣掃出 6 個命中，其中 1 個是真的撞題、其餘是同一組坐標出現在不相干的脈絡（也一併換掉，成本是零）。這條掃描寫成十行 JS 就跑得完，每一節完稿前都應該跑。
30. **把行內算式改寫成顯示型時，分數維持 `\frac`，不要改用 `\tfrac`**：`\frac` 在顯示型是 displaystyle（大分數），換成 `\tfrac` 會縮成跟行內一樣的小分數——寬度確實省了，但同一頁其他顯示型算式的分數還是大的，兩者並排一眼看得出不一致（`1-2-3` 實測 `\tfrac` 的 `mjx-mfrac` 高 27.3px，同頁 `.explanation-card` 裡的顯示型算式是 47.6px）。要省寬度就多拆一行，不要改分數的大小。**判斷放不放得下不要用眼睛猜**：把 `\frac` 版本塞進一個寬度等於目標容器 `clientWidth` 的隱藏 div 裡 `MathJax.typesetPromise()` 一次，比對 `scrollWidth` 與 `clientWidth`，相等就是放得下（`1-2-3`／`1-2-4` 實測 229px 容器內容剛好 229，本來就不必動分數）。
31. **算式溢出要掃的容器有四種，別只掃前兩種**：`.value-display`（階段十一、十五掃過）、`.explanation-desc`／`.explanation-box`（階段十八掃過）、**`.visual-feedback`（互動卡的回饋區）**、**`.summary-card`／`.context-story`（核心回顧）**。後兩者到 2026-09-04 才第一次掃，全冊抓到 7 條，所以在那之前的「第一冊算式溢出歸零」都不涵蓋它們。兩者的表現不同：`.visual-feedback` 有 `overflow-x: auto`，溢出會冒出捲軸（不會被裁）；`.summary-card` 是 `overflow: visible`，算式會直接壓出卡片邊界（`1-3-1` 實測 32px）。**都不是內容消失，但都要修**——捲軸比它要捲的內容還顯眼，壓出邊界則會蓋到卡片外。修法是共用的 `wbrEq()`（開發約束 24），核心回顧那種手寫在 HTML 裡的則自己以 `<wbr>` 接段。**`.visual-feedback` 與 `.value-display` 一樣，只掃初始狀態會漏掉一半**：`1-2-3` 重點 6 那兩條要點到「果汁」「圖書館」按鈕才出現、`1-3-1` 重點 4 那條要切到「15 − (2/3)x」才出現，初始狀態全都乾淨。**另外，畫面上的中文說明不要包進 `\text{}`**：`1-3-3` 重點 7 的「（乙沒有比甲快，情境不成立）」原本包在 `\text{}` 裡送給 MathJax，變成不折行的 `mjx-container`，414px 下撐出 24px 捲軸；那不是算式，直接當純文字輸出就換得了行。
32. **整節重畫插圖前，先看該節的「暖身漫畫」與「內文情境詞頻」再定主題**：〈跨專案依賴〉只規定「漫畫要對齊插圖」，反方向沒寫——於是 2026-09-05 重畫 `1-1-2` 時，我自己挑了「大樓電梯樓層」當主軸，六張生完才發現該節漫畫是「咖啡廳冷凍櫃調溫」、內文「溫度」出現 20 次而「電梯」只有 2 次，等於憑空造出第三個情境，角色（黑髮制服女學生 vs 漫畫的棕髮圍裙店員＋灰貓）也對不上，**16 張候選整批作廢**。作法是動筆前先 `grep -o` 數內文的情境詞（溫度／樓層／海拔／收支……）取最高的那個當主軸，再把漫畫的角色**逐項抄進提示**（髮色髮型、服裝、圍裙顏色、動物的花色與項圈配件）。順帶：**改完圖要回頭 grep 內文有沒有描述舊插圖的句子**（「如上圖的拔河」這類），本次查過確認為零才敢換。
33. **生圖提示要用「可驗證的幾何」描述關係，不要用關係詞；要數的東西壓到每組 ≤8、組數 ≤3**：gpt-image-2 抓不到 interlocking／ten times wider／six of eight 這類詞，全部第一輪就失敗——齒輪畫成齒尖對齊還隔著空隙、十倍寬只做到 4~5 倍、8 等分取 6 畫成取 7。改寫成「一齒插進對方兩齒之間的凹縫、兩個輪廓互相重疊、絕無空隙」「小平台幾乎只比中間的電梯管寬一點，十個並排才等於下層」「那兩塊素色相鄰、合起來剛好等於另一個塔的那一塊素色四分之一」之後全部一次命中。數量同理：`1,2,4,8` 四座塔連兩輪畫成 `1,3,5,9`／`1,3,6,10`，改成 `2,4,8` 三座立刻正確——這是〈跨專案依賴〉「不超過 9」的下一層，**組數也要少**。需要量出「兩段等長」「中點」這類關係時 `--n 3`，兩張往往都差幾個百分點。反過來，**數線標負號意外地穩**（`-3 -2 -1 0 1 2 3` 三張全對），不必迴避。**每一張的提示都要完整帶上該節的角色設定，不能因為「這張沒有主角」就省略**——`2-1-1` 的分類箱那張只寫了場景，模型就自己生出一隻打領結的鴿子當售票員，跟同節其他七張對不上。提示裡要正面寫明角色，也要反面排除（`no birds, no animals wearing clothes, no other characters`）。**要模型寫出數字時，位數越少越穩**：同一張圖「槽裡插滿鉛字」不指定內容會被畫成 `0000`（讀起來像零元），改成「左槽兩枚鉛字 6 和 0、右槽 3 和 0」就一次正確。**而且要寫死「印一次」**——只說「左牌是 60、右牌是 30」，`2-1-2` 的漫畫兩張候選都把它畫成翻牌機那樣上下重複兩排（`60/60`、`30/30`），讀起來像 `6060`。提示要補「每個數字只印一次，置中在自己那張卡上，上下留白，不重複、不鏡像，也不可以有橫桿或欄杆壓過數字」，一輪就對。**要表達「A 對應到 B」時用「對齊」而不是「連線」**：畫虛線引導線時模型會把線接到錯的對象（四張票根配三個箱子全錯），改寫成「每張票根平放在它所屬那一格的正下方、不要畫任何連線」就對了。**要讓箭頭／弧線的端點落在「指定的那一格刻度」上，先在那幾格畫實心圓點，再要求線去接圓點**：`1-1-2` 重點 1 的兩段跳躍弧線連三輪十張都把端點畫在刻度之間（起跳點固定落在 `0` 右邊 0.5～0.8 格），補上「虛線垂足」「八格等距」都沒救。但**同一批的 `temp_change`／`temp_midpoint`，紅點藍點卻每張都精準落在刻度上**——差別在於圓點是一個**畫得出來的實體物件**，而「刻度上方的空白處」不是。改寫成「`0` 上有綠點、`-2` 上有空心白點、`-6` 上有藍點，第一段弧線由綠點連到白點、第二段由白點連到藍點」，一輪四張就有兩張三點全中（實測弧長 212px : 456px，對上刻度的 215px : 453px）。同理，要表達「兩點的距離」「中點」時也先把兩端點畫成圓點再拉捲尺。**要表達「A 剛好落在 B 上」時，寫成「穿過去再出來」而不是「在上面」**：`2-2-2` 的重點 6 要畫「紅旗在線上、藍旗不在線上」，第一輪寫 `its foot planted ON that ink line` 兩張候選都把兩支旗畫在線的附近而沒有真的壓上去；改寫成「線從旗的黃銅底座<strong>下面穿過去、再從另一邊直直出來</strong>」，而「不在線上」那支則寫「底座與線之間隔著<strong>一大片空白的羊皮</strong>」，一輪就命中（實測紅旗底座與線的落差 0.4px、藍旗離線 91px）。同一個道理，「兩把尺交叉」要寫成「每一把都往兩端<strong>伸出對方之外</strong>」，否則模型會畫成 L 形的直角尺；「兩條線一條通過原點、一條不通過」要寫成「第二條與第一條<strong>同方向但整條往旁邊挪</strong>，中間隔著一大片空白」，否則兩條都會穿過中央那枚圓釘。**要表達「兩排一樣長」時，寫成「兩排合起來剛好是一個完整的長方形」**：`2-3-1` 的 `ratio_times` 第一輪只寫「左端對齊、右端也對齊」，兩張候選都把左端錯開、只讓右端貼著尺，**實測總長差 7.4%**（等於「一樣長」這句話在畫面上是假的）；改寫成「兩排的左端與右端都落在同一條垂直線上，兩排合起來剛好是一個完整的長方形，任何一排都不可以超出另一排」之後，三張候選全中。**要比較「兩根的長度」時，畫面裡不要放尺**：同一節的 `ratio_simplest` 第一輪把尺畫成了其中一根，看起來只剩一根木條，比根本讀不出來；提示裡寫死「墊子上沒有尺、沒有直尺、沒有任何其他工具」就對了。**同一件道具不要同時給兩個互斥的狀態**：`ratio_cross` 的角色設定寫「護目鏡推在額頭上」，而該張的動作又寫「護目鏡拉下來遮住眼睛」，模型的解法是**畫兩副**（一副在額頭、一副在眼睛，或額頭一副再加一副圓眼鏡）；衝突時要明講「護目鏡仍然推在額頭上，眼睛前面什麼都沒有，不戴眼鏡也沒有第二副護目鏡」。
34. **數線一律只在正向那端畫箭頭，負向那端平切**：數線的定義是「在直線上取一點為原點，訂定正向與單位長度」，那個箭頭是用來**標示正向**的，不是用來表示「兩頭無限延伸」——所以兩端都畫箭頭是錯的，一端都不畫也是錯的。兩節的 `canvas.js` 本來就照這條寫（`1-1-2/canvas.js` 的數線繪製處留有註解 `Right arrow only, left is a flat end, per Taiwanese junior high definition`），**歷來出錯的都是插圖**：`1-1-1` 實測 `capybara_opposite`／`capybara_summary` 兩端都有箭頭、`capybara_distance` 兩端都沒有，同一頁的 canvas 與插圖互相矛盾。生圖提示要把它寫死成幾何（照〈開發約束〉33）：「The line has ONE arrowhead only, at its right end. The left end simply stops flat, square-cut, with absolutely no arrowhead there.」實測這樣寫十張全中。**溫度刻度條、量尺這類「本質上是數線」的道具同樣適用**。⚠️ **但寫死幾何仍不保證一次過，一定要逐張量端點**——`2-2-1` 的漫畫候選 2 把幾何照抄了，x 軸右端有箭頭沒錯，左端卻又多長了一個往左的箭頭（被貓的鬍鬚蓋住，縮圖上看不出來）；`chart_axis` 的候選 1 更畫出**兩條鉛垂線**，等於沒有共同原點。判讀方式是把線端裁下來放大 4～6 倍看，或沿著軸線逐點量前景厚度：平切端維持線寬（2px 上下），有箭頭的那端會從尖端往內鼓到 10px 以上再收回去。**另外，canvas 上的箭頭要自己畫，不要用 `math-canvas.js` 的 `drawArrow`**：它的箭頭長 7px、半角 0.4 rad，實際只有 **5.4px 寬**，在〈開發約束 13〉那個 0.7 倍的投影縮水下細得像一根刺，而這個箭頭正是用來標示正向的唯一記號，讀不出來等於這條規則沒生效。`2-2-1/canvas.js` 的 `axisArrow()`（長 13px、半寬 5.5px 的實心三角形）可以照抄

35. **概念畫不出可驗證的幾何時，插圖就退回純情境圖，不要硬套比喻**：〈開發約束〉28、33 要求「工具形狀要能產生畫面上的結果」「用可驗證的幾何描述關係」，但有些重點根本沒有幾何可畫。`1-1-2` 重點 5 教的是**符號操作的規則**（絕對值內部先算完、負號不能鑽進絕對值），不是空間關係；硬要圖解就會退化成畫「絕對值的定義」（\(-5\) 投進去、\(5\) 吐出來），而那是 `1-1-1` 的內容——連換兩代（`temp_absolute` → `abs_machine`）都沒逃掉，第二代還因為機器只是個沒特徵的箱子、兩根黑槓中間又空著，連「那兩根槓是絕對值符號」都讀不出來。判準是先問**這個重點有沒有位置、數量、大小、能不能穿過這類畫得出來的東西**；沒有就讓插圖退回**純情境圖**——該節場景裡的一個日常片刻，畫面上不出現任何數字、符號、箭頭與文字（生圖提示要逐項排除，否則模型會自己加上招牌與標價），只要求角色與場景跟該節漫畫一致（〈開發約束〉32）。教學責任交給同一個重點的互動卡與重點整理，它們本來就在承擔。⚠️ 這表示**「每個重點都要配一張圖解該重點的插圖」不是硬性要求**——牽強的比喻比沒有比喻更糟，課堂投影時學生會先讀圖，讀出錯的東西比讀不到東西傷害更大。**有幾何可畫、但連兩輪都畫不出來，也照這條退**：`2-4-1` 重點 5 的「頭頂落在兩根橫桿之間」，第一輪寫立體橫桿、第二輪改成木板上畫兩條紅線並用肩膀與「頭頂上方一個手掌高」定位，10 張全部失敗（只有一根、落在肩膀或下巴、畫成十字架）；第三輪不再改寫提示，直接改用同節另一張純情境圖。另外，**提示裡不要出現「畫著角色的道具」**（小丑立牌、吉祥物看板）：Z-Image 會把它畫成穿戲服的第三個人，拿掉角色、只寫「紅白條紋柱子加一根橫桿」才乾淨

36. **一節 16 題的正解不可以集中在同一個字母**：`2-1-2` 完稿時 16 題有 **14 題正解是 A**——每一題都是「先寫對的做法，再逐條解釋別的選項錯在哪」，正解自然落在第一個位置。學生用不著讀題就猜得到，形成性評量整節失效。**完稿前數一次正解的字母分布，A/B/C/D 盡量各佔四分之一**。搬動時要三件事一起改：選項內容互換、`canvas.js` 的 `answers` 表、解析裡提到的每一個代號（含 `\text{A}：` 這種寫在 `aligned` 裡的）。代號替換用「前後都不是英文字母、前面不是反斜線」的規則做兩兩互換，才不會動到 `\Rightarrow`、`\dfrac` 這些指令。改完必須從瀏覽器把**答案表、選項字母、解析的「答案為 X」**三方重新對一次。

37. **LaTeX 裡的不等號一律寫 `\lt`、`\gt`、`\le`、`\ge`，不要直接打 `<`、`>`**：這些算式是經 `innerHTML` 或靜態 HTML 送出去的，`\(x<y\)` 裡的 `<y` 會被 HTML 解析器當成標籤開頭，整段算式直接消失，console 不會報錯。按鈕上的純文字標籤用 `&lt;`／`&gt;`。**共用檔的 `wbrEq()` 也不會在不等號前斷開**（它只認 `=`、`+`、`-`），連寫的 `-2 \lt x \le 3` 會整條包成一段，窄螢幕下放不下。共用檔的 `wbrRel()`（2026-09-13 由 `2-4-1` 的 `iqWbr()` 抽出）會在頂層的 `\le`／`\ge`／`\lt`／`\gt`／`\ne`／`=` 前斷開（`\left(` 的 `\le` 後面接字母 `f`，不會被誤認），吃的是裸 LaTeX。**它不會在括號裡斷開**：`2-4-2` 的點心站把 `(55 + 60 + 65 + 80 + x) \times 0.8` 整段交給它，414px 下溢出 68px；括號裡的加項要自己用 `<wbr>` 接段。在數線上畫解的 `numLine`／`numLineEnd`／`numLineRay`／`numLineSeg` 也在共用檔

## 跨專案依賴

- 教材頁最上方的「課前暖身漫畫」**固定調用** `teaching-comic` 專案的 `comic-generator` 技能：
  `C:\Users\chang\我的雲端硬碟\agents\teaching-comic\skills\comic-generator\SKILL.md`
- 細節規範見 `.agents/skills/math-interactive-material/SKILL.md` 第 3.1 節；本專案不再自行實作漫畫生圖或加字流程
- **漫畫前處理不要裁切四格內容**：比例不合改補白邊，否則會切掉邊緣角色
- 漫畫只有 `_raw.png` 會呼叫生圖 API 計費，`_prepped`／`_normalized`／`_final` 都是本機處理，改對白重跑不用再花錢
- 風格規則為依小節指定，不再全站固定水豚風
- **漫畫的角色設定要與該節插圖對齊，而且要在生圖提示裡逐項寫死**：風格關鍵字相同並不保證角色相同——`1-3-2` 的插圖是黑髮男孩配黃銅發條貓，漫畫卻生成棕色捲髮男孩配藍綠眼貓，兩者擺在同一頁一眼看得出不是同一套教材。動筆前先看過該節既有插圖，把**髮色與髮型、隨身配件、服裝、動物角色的材質與尾巴形狀、眼睛顏色**逐項寫進提示，並指明畫風媒材（例：黑墨線＋水彩暈染，不是數位卡通）
- **對話泡一律由生圖階段畫出**（生圖提示要空白泡，不要再寫 `no speech bubbles`），後製腳本預設只排字；泡不堪用就重生原圖，不要用 `-DrawBubbles` 補框
- **對白座標一律從實際的 `_normalized.png` 量測**泡的內緣，不要從腳本參數或原圖推算
- **`bubbles.json` 的 `w`／`h` 要給泡的完整內緣，不要預先內縮**：`add_captions_json.ps1` 自己會加 padding，先內縮會變成縮兩次，字級被壓到最小值仍排不下而報錯
- **`add_captions_json.ps1` 用矩形 bbox 判定重疊**：旁白方框與相鄰橢圓泡只要 bbox 的角落交疊就會報 `Bubble N and bubble M overlap`，即使兩個泡在畫面上根本沒碰到。把靠得比較近的那個框寬度縮 15～20px 就好，**不要加 `-AllowOverlap`**（那會讓真正的重疊也被放行）
- **挑生圖候選要看泡的內緣尺寸，不是只看畫面好不好看**：同一次呼叫產生的候選畫面品質往往相近，但泡的大小可能差很多（實測第 1 格旁白框 154×49 vs 180×55），小的那張排不進 18px 字級
- **`add_captions_json.ps1` 的字級自動縮放只在「連折行都放不下」時才降級**，文字太寬時它預設是折行而不是縮小；長數字串想排成一行得自己把 `font_size` 試小
- **畫面要表現「尺寸變化」時，生圖提示要把每一格的相對尺寸寫死**：只說「摺了幾次」模型會畫成同尺寸的紙疊，面積不會跟著變小。要逐格寫明（例：第 2 格是原張的四分之一面積、第 3 格只有巴掌大但明顯變厚、第 4 格兩者並排對比），數學上的比例才會出現在畫面上
- **`normalize_comic.ps1` 與 `add_captions_json.ps1` 覆蓋既有輸出要加 `-Force`**：重生原圖後沿用同一個檔名會被擋下來
- **不要用 gpt-image-2 的遮罩改圖（`--mask`）做局部修圖**：實測會無視遮罩把整張重畫，四格結構全毀；要改一個小物件也一律整張重生，並在同一次呼叫多生幾張候選挑選
- **挑漫畫候選時，道具的外形也要逐格比對，不能只數數量**：`2-4-1` 第一版漫畫第 4 格的小丑立牌少了往右伸出的橫桿（少女站的位置正好佔住它），上線後才被使用者抓到。想從第 3 格搬移補上時，能露出的空間只剩約 17px（頁面上約 9px），最後整張重生。提示裡要寫死「四格的某道具完全一模一樣、每一格都看得到」，並把會擋住它的角色位置一起寫開（例：「第 4 格少女已經離開柱子，走到畫面中間偏右」）
- **挑漫畫候選時要逐格數同一種物件的數量，四格必須一致**：`2-2-2` 的候選 2 泡的內緣每個都比候選 1 大 5～20%，照「挑泡最大的那張」會選它——但它第 2、3 格是 3 支旗、第 4 格卻長成 5 支，而第 4 格的旁白正是「兩支旗就畫得出整條線」，畫面與結論互相打架。**先數物件、再比泡的大小**；泡只要都過了下限（一般 240×110、`shout` 300×150）就不是挑選依據
- **不要把不同候選圖的格子拼在一起**：`1-3-2` 試過拿 A 圖的第 1 格配 B 圖的第 3 格，接縫雖然落在黑框上，但**背景、光線、道具外觀、角色比例全都對不上**，一眼就看得出來，課堂上比數量錯更礙眼。四格必須來自**同一次生成**，某一格不合格就整張重生
- **漫畫裡要數的物件數量不要超過 9**：gpt-image-2 畫「14 個砝碼」實測連跑六輪 24 張沒有一張對過（畫成 10、11、15、18 都有），9 以內才穩。**課本的數字太大時，改用同構的小數字**——`1-3-2` 把 \(3x+5=14\) 改成 \(2x+3=9\)（結論同樣是 \(x=3\)），四格數量一次到位，再由頁面導言補一句「課本的 \(3x+5=14\) 是同一回事」把兩者接起來
- **要數的東西一律「平放排成一排或方陣」，不要疊高**：同一批候選裡，平放的一排 5 個、3×3 方陣模型幾乎都畫對，疊成一疊的「5 片」卻常變成 4 片或 7 片（側視的邊緣不好數）。生圖提示要寫死「lying flat, side by side, slightly apart, never stacked」
- **物件外觀要在提示裡寫死並要求四格一致**：只說「brass disc」模型會一格畫平放、一格畫立起；要逐項寫明尺寸、朝向、材質與擺法（例如「同直徑同厚度的圓餅，一律平放，絕不立起或傾斜」），角色與道具也要加一句「四格是同一張桌子、同一個機位拍出來的」，否則視角會忽遠忽近
- **天平盤內有立體物件時，生圖提示指定「檯面式上皿天平（Roberval balance）」**：避免模型畫吊鍊時與立體物件碰撞而產生歪斜、單鍊或力學不平衡的瑕疵
- **四格後期物件數量減少時，提示需強調「維持原小尺寸、盤面留白、絕不放大」**：如第 4 格由 2 盒變 1 盒時，模型容易自動放大單一物件填滿畫面，須明確寫死 miniature 並保留周圍大面積留白
- **生圖提示應簡化背景與微小機件，避免密集小齒輪或複雜鐘錶機構**：模型在微小尺度（如天平轉軸、背景遠景）繪製密集齒輪容易產生破圖與金屬熔接雜訊；指定實心立柱幾何與簡潔木質背景能讓畫面乾淨、焦點明確
- **「考卷檢討」不在本 repo**：2026-08-30 拆成獨立專案 `exam-review`，2026-09-11 再併入 `class-score`（共用登入），`exam-review` 已退場。入口是側邊欄的「課堂表現登記系統」卡片，登入後從頁首開啟，側邊欄不再另放考卷檢討的卡片。它需要 GAS 後端做密碼驗證，與本站「無伺服器」的架構不同，**不要把它搬回來**，也不要在本 repo 放任何考卷資料夾的 ID
- `wordcloud.html` **固定由全域 `word-cloud-page` 技能模板產生／同步**：`C:\Users\chang\.agents\skills\word-cloud-page\SKILL.md`
- 文字雲固定使用 `CLOUD_ID = teaching_web`，資料位於 `clouds/teaching_web/words/`；同步新版模板時保留本專案的 `background.png` 背景客製化

## 部署

- GitHub Pages 分支部署（classic）：來源為 `main` 分支根目錄，**push 到 `main` 就會自動發布**
- 沒有 `.github/workflows` 是正常的，設定在 repo Settings → Pages，不要因為找不到 workflow 就以為沒有自動部署
- push 前先確認資產路徑為相對路徑，避免在子路徑 `/teaching-web/` 下失效
- 正式 Firebase 互動頁不要用自動化瀏覽器驗收；App Check 可能拒絕低可信用戶端，改由使用者本人用一般瀏覽器實測或查看 App Check Metrics
